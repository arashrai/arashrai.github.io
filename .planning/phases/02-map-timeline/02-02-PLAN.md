---
phase: 02-map-timeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - narsh2026/our-story/carousel.js
  - narsh2026/our-story/timeline.js
  - narsh2026/our-story/index.html
  - narsh2026/our-story/our-story.css
  - narsh2026/images/story/placeholder-1.svg
  - narsh2026/images/story/placeholder-2.svg
  - narsh2026/images/story/placeholder-3.svg
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03, TIME-04]

must_haves:
  truths:
    - "User can view 1-3 photos at each stop via a swipeable/clickable carousel"
    - "User can click timeline bar dots to jump directly to any stop"
    - "Mobile users experience cross-fade transitions between map view and content view"
    - "Timeline bar shows which stops have been visited and which is currently active"
    - "Photo carousel shows dot indicators and navigation arrows for multi-photo stops"
  artifacts:
    - path: "narsh2026/our-story/carousel.js"
      provides: "Touch-swipe photo carousel with arrow navigation and dot indicators"
      contains: "NARSH_CAROUSEL"
    - path: "narsh2026/our-story/timeline.js"
      provides: "Clickable timeline bar with active/visited state management"
      contains: "NARSH_TIMELINE"
    - path: "narsh2026/images/story/placeholder-1.svg"
      provides: "Placeholder image for photo carousel testing"
    - path: "narsh2026/images/story/placeholder-2.svg"
      provides: "Second placeholder image"
    - path: "narsh2026/images/story/placeholder-3.svg"
      provides: "Third placeholder image"
  key_links:
    - from: "narsh2026/our-story/timeline.js"
      to: "narsh2026/our-story/scroll-controller.js"
      via: "onDotClick callback calls NARSH_SCROLL.scrollToStop(index)"
      pattern: "NARSH_SCROLL\\.scrollToStop"
    - from: "narsh2026/our-story/carousel.js"
      to: "narsh2026/our-story/index.html"
      via: "loadPhotos called from onStopChange callback with stop.photos"
      pattern: "NARSH_CAROUSEL\\.loadPhotos"
    - from: "narsh2026/our-story/index.html init script"
      to: "narsh2026/our-story/timeline.js"
      via: "NARSH_TIMELINE.init builds dots, setActive/setVisited called on stop change"
      pattern: "NARSH_TIMELINE\\.setActive"
---

<objective>
Add photo carousel and clickable timeline bar to complete the map timeline experience, with responsive mobile cross-fade transitions, placeholder images, and full keyboard/touch accessibility.

Purpose: This plan completes the "Our Story" page by adding the two remaining interactive components (photo carousel and timeline bar) and polishing the mobile experience with cross-fade between map and content views. After this plan, all TIME requirements are fully delivered.

Output: Complete map timeline page with photo carousel (swipe/arrows/dots), clickable timeline bar (jump-to-stop), placeholder SVG images, and polished responsive layout.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-map-timeline/02-CONTEXT.md
@.planning/phases/02-map-timeline/02-RESEARCH.md
@.planning/phases/02-map-timeline/02-PATTERNS.md
@.planning/phases/02-map-timeline/02-UI-SPEC.md
@.planning/phases/02-map-timeline/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From narsh2026/our-story/story-data.js:
  NARSH_STORY_DATA.STOPS — array of stop objects
  Each stop: { id, owner, location, coords, zoom, year, narrative, photos: [{src, alt}], isConvergence }
  NARSH_STORY_DATA.getArashCoords() -> [[lng, lat], ...]
  NARSH_STORY_DATA.getNatalieCoords() -> [[lng, lat], ...]

From narsh2026/our-story/map.js:
  NARSH_MAP.init(containerId) -> Promise<map|null>
  NARSH_MAP.flyToStop(coords, zoom) -> void
  NARSH_MAP.updateLines(stopIndex, stops) -> void
  NARSH_MAP.updatePins(stopIndex, stops) -> void
  NARSH_MAP.playBowAnimation(convergenceCoords) -> void

From narsh2026/our-story/scroll-controller.js:
  NARSH_SCROLL.init(stops, onStopChange) -> void
  NARSH_SCROLL.scrollToStop(index) -> void
  NARSH_SCROLL.getCurrentIndex() -> number

From narsh2026/our-story/our-story.css:
  .story-panel, .stop-heading, .stop-narrative, .stop-year, .stop-carousel
  .timeline-bar, .map-container, .scroll-container
  .scroll-prompt, .map-loading, .map-error

From narsh2026/our-story/index.html:
  #stop-carousel div inside .story-panel (carousel mount point)
  #timeline-bar div (timeline mount point)
  Inline init script that wires onStopChange callback
</interfaces>
</context>

## Phase Goal

**As a** wedding guest, **I want to** scroll through an interactive world map that shows where Natalie and Arash grew up, **so that** I can experience their parallel life stories converging into a love story.

<tasks>

<task type="auto">
  <name>Task 1: Photo carousel module and placeholder images</name>
  <files>narsh2026/our-story/carousel.js, narsh2026/images/story/placeholder-1.svg, narsh2026/images/story/placeholder-2.svg, narsh2026/images/story/placeholder-3.svg, narsh2026/our-story/our-story.css</files>

  <read_first>
    - narsh2026/our-story/index.html (current state from Plan 01 — find #stop-carousel mount point, understand init script structure)
    - narsh2026/our-story/our-story.css (current state from Plan 01 — add carousel-specific styles)
    - narsh2026/our-story/story-data.js (read stop.photos structure: [{src, alt}])
    - narsh2026/nav.js (analog for IIFE + event listener pattern)
    - .planning/phases/02-map-timeline/02-RESEARCH.md (Example 3: Photo Carousel — touch swipe, goTo, updateUI)
    - .planning/phases/02-map-timeline/02-PATTERNS.md (carousel.js pattern assignment — IIFE, touch events, El suffix)
    - .planning/phases/02-map-timeline/02-UI-SPEC.md (carousel component spec: states, interaction, accessibility, sizing)
  </read_first>

  <action>
  Create narsh2026/our-story/carousel.js as NARSH_CAROUSEL IIFE module per D-09 (photo carousel with 1-3 photos).

  Private state: currentIndex (number), photos (array), plus El-suffixed DOM references: containerEl, trackEl, prevEl, nextEl, dotsEl, announceEl.

  init(containerEl): Query inside the container for .carousel-track, .carousel-prev, .carousel-next, .carousel-dots, .carousel-announce. Register click handlers on prev/next arrows. Register touch events on trackEl for swipe: touchstart captures startX (passive:true), touchmove tracks deltaX and applies translateX offset to track (passive:true), touchend evaluates deltaX — if abs > 50px, advance/retreat; else snap back. Register keydown on containerEl for left/right arrow key navigation (only when containerEl contains the focused element).

  loadPhotos(newPhotos): Set photos = newPhotos, reset currentIndex to 0. Build trackEl innerHTML with img elements: class="carousel-photo", src from photo.src, alt from photo.alt, loading="lazy". Call updateUI(). If photos.length is 0, hide the carousel container entirely.

  goTo(index): Clamp index between 0 and photos.length-1. Set currentIndex. Apply CSS transform on trackEl: translateX(-{currentIndex * 100}%) with transition 250ms ease. Call updateUI(). If prefers-reduced-motion, set transition to "none".

  updateUI(): Show/hide prev arrow (hidden at index 0), show/hide next arrow (hidden at last index). If photos.length <= 1, hide both arrows and dots container. Otherwise, build dots innerHTML: span.carousel-dot elements, add .active class to current index dot. Update announceEl text to "Photo {n} of {total}" for screen readers.

  Public return: { init, loadPhotos, goTo }.

  Create three SVG placeholder images at narsh2026/images/story/placeholder-1.svg, placeholder-2.svg, placeholder-3.svg. Each is a simple 600x400 (3:2 ratio) SVG with the site's dusty rose background color (#C9928E at 30% opacity on cream), centered text showing a location name: placeholder-1 shows "Photo coming soon", placeholder-2 shows "Photo coming soon", placeholder-3 shows "Photo coming soon". Use Playfair Display or fallback serif font. These serve as visual placeholders until real photos are added.

  Add carousel-specific CSS to narsh2026/our-story/our-story.css:
  .stop-carousel — container with overflow:hidden, border-radius: var(--radius-md), position:relative, background: var(--color-warm-white), aspect-ratio: 3/2
  .carousel-track — display:flex, transition: transform var(--transition-normal), width will be set by number of photos
  .carousel-photo — width:100%, flex-shrink:0, object-fit:cover, border-radius: var(--radius-sm), aspect-ratio: 3/2
  .carousel-prev, .carousel-next — position:absolute, top:50%, transform:translateY(-50%), min-width:44px, min-height:44px (WCAG touch target), background: var(--color-text-primary) at 8% opacity on hover (15% on active), border:none, cursor:pointer, border-radius: var(--radius-sm), z-index:2. Contain a CSS-drawn arrow (border-based chevron, var(--color-text-primary)). .carousel-prev at left: var(--space-sm), .carousel-next at right: var(--space-sm).
  .carousel-dots — display:flex, justify-content:center, gap:var(--space-xs), padding:var(--space-sm) 0. Each .carousel-dot is 8px circle, background var(--color-dusty-rose) at 40% opacity, .carousel-dot.active background var(--color-terracotta).
  .carousel-announce — visually hidden (sr-only pattern: position:absolute, width:1px, height:1px, overflow:hidden, clip:rect(0,0,0,0)), aria-live="polite" attribute set in HTML.
  </action>

  <verify>
    <automated>grep -c "NARSH_CAROUSEL" narsh2026/our-story/carousel.js && ls narsh2026/images/story/placeholder-1.svg narsh2026/images/story/placeholder-2.svg narsh2026/images/story/placeholder-3.svg && grep -c "carousel-track" narsh2026/our-story/our-story.css</automated>
  </verify>

  <acceptance_criteria>
    - narsh2026/our-story/carousel.js contains const NARSH_CAROUSEL = (() => { with "use strict"
    - carousel.js exports init, loadPhotos, goTo
    - carousel.js registers touchstart, touchmove, touchend with { passive: true }
    - carousel.js registers keydown handler for left/right arrow keys
    - carousel.js updateUI hides arrows/dots when photos.length <= 1
    - carousel.js announceEl updates with "Photo N of M" text
    - carousel.js goTo checks prefers-reduced-motion and disables transition if matched
    - Three placeholder SVGs exist at narsh2026/images/story/ with 600x400 (3:2) dimensions
    - our-story.css has .carousel-prev and .carousel-next with min-width:44px and min-height:44px
    - our-story.css has .carousel-dots with 8px dot circles
    - our-story.css has .carousel-announce as visually hidden sr-only
  </acceptance_criteria>

  <done>Photo carousel component handles 1-3 photos with swipe, arrow, and keyboard navigation. Three placeholder SVGs are ready for the story data. Carousel CSS is styled with design tokens and WCAG-compliant touch targets.</done>
</task>

<task type="auto">
  <name>Task 2: Timeline bar module, mobile cross-fade, and page wiring</name>
  <files>narsh2026/our-story/timeline.js, narsh2026/our-story/index.html, narsh2026/our-story/our-story.css</files>

  <read_first>
    - narsh2026/our-story/index.html (current state — understand init script, #timeline-bar mount point, script loading order)
    - narsh2026/our-story/our-story.css (current state — add timeline and mobile cross-fade styles)
    - narsh2026/our-story/story-data.js (STOPS structure with location and year fields)
    - narsh2026/our-story/scroll-controller.js (NARSH_SCROLL.scrollToStop API for timeline dot click handling)
    - narsh2026/our-story/carousel.js (just created in Task 1 — NARSH_CAROUSEL.init and loadPhotos API)
    - narsh2026/nav.js (analog for DOM interaction pattern, El suffix, event listeners)
    - .planning/phases/02-map-timeline/02-PATTERNS.md (timeline.js pattern assignment — IIFE, DOM query + class toggling pattern from nav.js)
    - .planning/phases/02-map-timeline/02-UI-SPEC.md (timeline bar spec: dot sizes, active/visited states, year labels, mobile layout, cross-fade)
  </read_first>

  <action>
  Create narsh2026/our-story/timeline.js as NARSH_TIMELINE IIFE module per D-08 (clickable timeline bar).

  Private state: dotElements (array of button elements), currentIndex (number).

  init(stops, onDotClick): Build the timeline bar contents inside the #timeline-bar container. For each stop, create a button.timeline-dot element with aria-label="{stop.location}, {stop.year}". Add a span.timeline-year beside key dots (every few stops or at year boundaries to avoid overcrowding — show year labels at first stop, convergence stop, and last stop at minimum). Register click handler on each dot that calls onDotClick(index). Store references in dotElements array.

  setActive(index): Remove .active class from all dots, add .active to dotElements[index]. Update aria-current="step" on active dot, remove from others.

  setVisited(upToIndex): Add .visited class to all dots from 0 to upToIndex.

  Public return: { init, setActive, setVisited }.

  Add timeline-specific CSS to narsh2026/our-story/our-story.css:
  .timeline-dot — 8px width/height circle (border-radius:50%), background var(--color-dusty-rose) at 40% opacity, border:none, cursor:pointer, padding:0, position:relative. The actual tap target is larger: use a ::before pseudo-element sized 44x44px centered on the dot (transparent hit area). Transition: transform var(--transition-fast), background var(--transition-fast).
  .timeline-dot.visited — background: var(--color-dusty-rose) at full opacity.
  .timeline-dot.active — width:12px, height:12px, background: var(--color-terracotta), transform: scale(1) (reset any default scale).
  .timeline-dot:hover — show tooltip via ::after pseudo-element or a title attribute with stop.location (use title attribute for simplicity).
  .timeline-dot:focus-visible — outline: 2px solid var(--color-terracotta), outline-offset: 2px.
  .timeline-year — font-size: var(--font-size-sm), color: var(--color-text-secondary), white-space:nowrap.

  Add mobile cross-fade CSS to our-story.css per D-07:
  Below 768px, the .story-panel and .map-container alternate visibility. Add a .mobile-show-content class that, when applied to the page wrapper: fades .map-container to opacity:0 and .story-panel to opacity:1 (transition: opacity 300ms ease). Without the class, .map-container is opacity:1 and .story-panel is opacity:0 on mobile. This class will be toggled by the scroll controller/init script to alternate between map and content views at each stop.

  Update narsh2026/our-story/index.html:
  1. Add carousel.js and timeline.js script tags in the correct load order (after scroll-controller.js, before inline init script):
     <script src="/narsh2026/our-story/carousel.js"></script>
     <script src="/narsh2026/our-story/timeline.js"></script>

  2. Add carousel HTML structure inside #stop-carousel:
     <div class="carousel-track"></div>
     <button class="carousel-prev" aria-label="Previous photo"></button>
     <button class="carousel-next" aria-label="Next photo"></button>
     <div class="carousel-dots"></div>
     <div class="carousel-announce" aria-live="polite"></div>

  3. Update the inline init script to wire carousel and timeline:
     After map init succeeds:
     - Call NARSH_CAROUSEL.init(document.getElementById("stop-carousel"))
     - Call NARSH_TIMELINE.init(NARSH_STORY_DATA.STOPS, (index) => NARSH_SCROLL.scrollToStop(index))
     In the onStopChange callback, add:
     - NARSH_CAROUSEL.loadPhotos(stop.photos)
     - NARSH_TIMELINE.setActive(newIndex)
     - NARSH_TIMELINE.setVisited(newIndex)
     - On mobile (<768px), toggle .mobile-show-content class: add class 800ms after stop change (after map flyTo is 40% done), remove class when stop change begins (so map is visible during flyTo). Use a setTimeout keyed to the stop transition.

  4. Ensure the initialization handles the "first stop" edge case: trigger onStopChange(0, -1) immediately after scroll controller init so the first stop's content appears without requiring any scroll.
  </action>

  <verify>
    <automated>grep -c "NARSH_TIMELINE" narsh2026/our-story/timeline.js && grep -c "NARSH_CAROUSEL" narsh2026/our-story/index.html && grep -c "carousel.js" narsh2026/our-story/index.html && grep -c "timeline.js" narsh2026/our-story/index.html && grep -c "timeline-dot" narsh2026/our-story/our-story.css && grep -c "mobile-show-content" narsh2026/our-story/our-story.css</automated>
  </verify>

  <acceptance_criteria>
    - narsh2026/our-story/timeline.js contains const NARSH_TIMELINE = (() => { with "use strict"
    - timeline.js exports init, setActive, setVisited
    - timeline.js init creates button.timeline-dot elements with aria-label="{location}, {year}"
    - timeline.js setActive adds/removes .active class and aria-current="step"
    - timeline.js setVisited adds .visited class to dots 0 through upToIndex
    - timeline.js dot click calls onDotClick(index) callback
    - index.html loads carousel.js and timeline.js scripts before inline init
    - index.html #stop-carousel contains carousel-track, carousel-prev, carousel-next, carousel-dots, carousel-announce elements
    - index.html inline script initializes NARSH_CAROUSEL.init and NARSH_TIMELINE.init
    - index.html inline script calls NARSH_CAROUSEL.loadPhotos(stop.photos) in onStopChange
    - index.html inline script calls NARSH_TIMELINE.setActive(newIndex) in onStopChange
    - index.html inline script triggers initial onStopChange(0, -1) after scroll controller init
    - our-story.css .timeline-dot has 44px tap target via ::before pseudo-element
    - our-story.css .timeline-dot.active has background var(--color-terracotta) and 12px size
    - our-story.css .timeline-dot:focus-visible has 2px terracotta outline
    - our-story.css contains .mobile-show-content class for cross-fade on mobile
    - our-story.css mobile cross-fade uses opacity transitions at 300ms
    - All JS uses const, double quotes, semicolons, 2-space indent, El suffix
  </acceptance_criteria>

  <done>Timeline bar renders clickable dots for all stops with active/visited states and year labels. Clicking a dot jumps to that stop. Photo carousel loads each stop's photos with swipe and arrow navigation. Mobile layout cross-fades between map and content views. All four TIME requirements are fully delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Touch Input -> Carousel | Touch event coordinates drive carousel position |
| User Click -> Timeline -> Scroll | Click on timeline dot triggers scroll position change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Carousel photo src from story-data.js | accept | Photos are static paths to repo-committed files. No user-supplied URLs. No XSS vector via innerHTML since img src values are from trusted const data. |
| T-02-06 | Denial of Service | Rapid timeline dot clicks flooding scrollToStop | mitigate | scrollToStop uses window.scrollTo with smooth behavior which the browser coalesces. Scroll controller only fires onStopChange on index change, not per-pixel. |
</threat_model>

<verification>
1. Open Our Story page, scroll through all stops
2. At each stop, verify photo carousel shows placeholder images with arrows/dots
3. Swipe left/right on mobile — carousel responds
4. Click arrow buttons — carousel navigates
5. Click timeline bar dots — page scrolls to that stop
6. Verify active dot highlighting and visited dot state
7. On mobile: verify cross-fade between map and content views
8. Keyboard: Tab to timeline dots, press Enter to jump; Tab to carousel arrows, press Enter to navigate
9. Screen reader: Verify "Photo 1 of 3" announcements on carousel change
10. Verify prefers-reduced-motion: no slide animations, instant carousel transitions
</verification>

<success_criteria>
- Photo carousel renders 1-3 placeholder images per stop with swipe, arrow, and keyboard navigation
- Carousel shows dot indicators for multi-photo stops, hides controls for single-photo stops
- Timeline bar shows all stops as clickable dots with active (terracotta, 12px) and visited (dusty rose) states
- Clicking a timeline dot scrolls to that stop and updates map, content, and carousel
- Mobile cross-fade transitions between map view and content view at each stop
- Screen reader announces photo position on carousel change
- All interactive elements have 44px minimum touch targets
- Focus-visible rings on all interactive elements
- prefers-reduced-motion disables slide and fade animations
</success_criteria>

<output>
After completion, create `.planning/phases/02-map-timeline/02-02-SUMMARY.md`
</output>
