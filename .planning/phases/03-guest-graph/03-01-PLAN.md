---
phase: 03-guest-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - narsh2026/our-people/guest-data.js
  - narsh2026/our-people/our-people.css
  - narsh2026/styles.css
  - narsh2026/our-people/graph.js
  - narsh2026/our-people/graph-ui.js
  - narsh2026/our-people/index.html
autonomous: true
requirements:
  - GRAPH-01
  - GRAPH-04

must_haves:
  truths:
    - "Guest can see a force-directed graph of ~20-30 placeholder guests with photo-circle nodes on the Our People page"
    - "Each node displays a circular photo or warm-toned initials fallback with a name label below"
    - "Natalie and Arash nodes are visually emphasized (larger, terracotta border)"
    - "Edges connect related guests with thin warm-brown lines"
    - "Soft colored cluster regions appear behind each relationship group"
    - "Guest can zoom (scroll/pinch) and pan the graph on desktop and mobile"
    - "Page uses shared auth guard, nav, and design system"
  artifacts:
    - path: "narsh2026/our-people/guest-data.js"
      provides: "NARSH_GUESTS IIFE with guest array, edges, groups, cities, households, family trees, and search/filter helpers"
      contains: "const NARSH_GUESTS"
    - path: "narsh2026/our-people/our-people.css"
      provides: "Page-specific styles for graph controls, canvas, nodes, loading/error states"
      contains: ".graph-canvas"
    - path: "narsh2026/styles.css"
      provides: "Graph-specific CSS custom properties added to :root"
      contains: "--color-node-default"
    - path: "narsh2026/our-people/graph.js"
      provides: "NARSH_GRAPH IIFE with D3 force simulation, SVG rendering, photo nodes, edges, cluster regions, zoom/pan"
      contains: "const NARSH_GRAPH"
    - path: "narsh2026/our-people/graph-ui.js"
      provides: "NARSH_GRAPH_UI IIFE with minimal init wiring"
      contains: "const NARSH_GRAPH_UI"
    - path: "narsh2026/our-people/index.html"
      provides: "Complete page shell with graph controls, SVG canvas, D3 CDN, auth guard, nav"
      contains: "cdn.jsdelivr.net/npm/d3@7"
  key_links:
    - from: "narsh2026/our-people/index.html"
      to: "narsh2026/our-people/guest-data.js"
      via: "script tag loading order"
      pattern: "guest-data\\.js"
    - from: "narsh2026/our-people/graph.js"
      to: "NARSH_GUESTS"
      via: "reads guest data for force simulation"
      pattern: "NARSH_GUESTS"
    - from: "narsh2026/our-people/index.html"
      to: "NARSH_GRAPH.init"
      via: "inline script calls init after auth"
      pattern: "NARSH_GRAPH\\.init"
---

<objective>
Deliver a working force-directed social graph on the Our People page. After this plan, a guest who enters the site password sees an interactive visualization of ~20-30 placeholder guests rendered as photo-circle nodes with edges and soft colored cluster regions, navigable via zoom and pan on both desktop and mobile.

Purpose: This is the core vertical slice for the guest graph feature -- data, rendering, and page shell in one deliverable.
Output: Six files (3 new JS modules, 1 new CSS, 1 HTML rewrite, 1 CSS modification) producing the working social graph view.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-guest-graph/03-CONTEXT.md
@.planning/phases/03-guest-graph/03-RESEARCH.md
@.planning/phases/03-guest-graph/03-PATTERNS.md
@.planning/phases/03-guest-graph/03-UI-SPEC.md

<interfaces>
<!-- Key patterns and contracts from the existing codebase that the executor needs -->

From narsh2026/our-story/story-data.js (analog for guest-data.js):
- IIFE pattern: const NARSH_STORY_DATA = (() => { "use strict"; ... return { STOPS, getArashCoords, getNatalieCoords, getStopById }; })();
- Data: array of objects with string id, typed fields, root-relative image paths
- Helpers: arrow functions that filter/find on the data array

From narsh2026/our-story/map.js (analog for graph.js):
- IIFE pattern: const NARSH_MAP = (() => { "use strict"; ... return { init, flyToStop, ... }; })();
- Module-level constants for colors and animation durations
- Private mutable state with let, null initialization (let mapInstance = null;)
- Reduced motion detection: window.matchMedia("(prefers-reduced-motion: reduce)").matches
- init(containerId) function as public entry point

From narsh2026/styles.css :root block (lines 4-43):
- Existing tokens: --color-terracotta (#C2704F), --color-golden (#D4A843), --color-dusty-rose (#C9928E), --color-cream (#FFF8F0), --color-warm-white (#FFFDFB), --color-text-primary (#3D2B1F), --color-text-secondary (#6B4F3A)
- Journey colors: --color-journey-arash (#2A9D8F), --color-journey-natalie (#D4A843)

From narsh2026/auth.js public API:
- NARSH_AUTH.requireAuth() -- redirects to gate if not authenticated, returns tier string
- NARSH_AUTH.applyTierVisibility(tier) -- removes auth-pending class, hides data-tier elements

From narsh2026/our-people/index.html (current placeholder):
- body class="auth-pending", header with site-header, nav with Our People active, main.page-content
- Scripts: auth.js, nav.js, inline requireAuth block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guest data module, graph CSS, and design tokens</name>
  <files>narsh2026/our-people/guest-data.js, narsh2026/our-people/our-people.css, narsh2026/styles.css</files>
  <read_first>
    - narsh2026/our-story/story-data.js (analog for IIFE data module pattern, object structure, helper functions)
    - narsh2026/our-story/our-story.css (analog for page-specific CSS: skip link, loading/error states, responsive breakpoints, reduced motion)
    - narsh2026/styles.css (existing :root tokens to extend with graph-specific custom properties)
    - .planning/phases/03-guest-graph/03-CONTEXT.md (D-01 through D-19 locked decisions)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (color tokens, spacing, typography, node sizes, cluster region specs)
    - .planning/phases/03-guest-graph/03-PATTERNS.md (exact analog patterns for each file)
  </read_first>
  <action>
    Create narsh2026/our-people/guest-data.js as a NARSH_GUESTS IIFE module following the NARSH_STORY_DATA pattern. Include:

    GROUPS array with objects {id, label, color}: "arash-family" (label "Arash's Family", color "#2A9D8F" per --color-journey-arash), "natalie-family" ("Natalie's Family", "#D4A843"), "college" ("College Friends", "#C9928E"), "meta" ("Meta Coworkers", "#C2704F"), "seattle-friends" ("Seattle Friends", "#B07D62").

    CITIES array with objects {id, label}: "seattle", "waterloo", "mumbai", "grand-cayman", "auckland", "new-york", "san-francisco".

    GUESTS array with ~25 placeholder guests. Each guest object: id (string), name (string), photo (null for most -- initials fallback), groups (string array of group ids), cities (string array of city ids), isCouple (boolean, true only for "natalie" and "arash"), funFact (string or null per D-12), connectionToCouple (string or null per D-12), householdId (string or null per D-05). Include the couple ("natalie" with name "Natalie Fleury", "arash" with name "Arash Rai") with isCouple: true and photo paths /narsh2026/images/people/natalie.jpg and /narsh2026/images/people/arash.jpg. Include guests with familyTree property: { family: "natalie"|"arash", parentId: string|null, generation: number }. Make ~8 guests in arash-family, ~8 in natalie-family, ~4 in college, ~3 in meta, ~2 in seattle-friends. Give ~5 guests householdId values to test household node combining. Give ~10 guests funFact strings and ~8 guests connectionToCouple strings.

    EDGES array with ~20 typed edges: { source: guestId, target: guestId, type: string }. Types: "couple" (natalie-arash), "sibling", "parent", "best-friend", "roommate", "introduced-us", "coworker". Ensure edges cover cross-group connections.

    HOUSEHOLDS array: ~3 household objects { id, members: [guestId, ...], displayName: string }.

    FAMILY_TREES object with "natalie" and "arash" keys, each a nested object suitable for d3.hierarchy(). Structure: { id, name, children: [...] }. Go 3 generations deep (grandparents -> parents -> the couple's generation). Ensure all family tree members also exist in the GUESTS array with matching ids.

    Helper functions: getGuestById(id), getGuestsByGroup(groupId), getGuestsByCity(cityId), searchGuests(query) -- following the arrow function filter/find pattern from story-data.js. Also add getSocialNodes() that returns a merged array where household members are combined into single nodes (per D-05), and getSocialEdges(socialNodes) that remaps edge source/target to household node ids.

    Return object: { GROUPS, CITIES, GUESTS, EDGES, HOUSEHOLDS, FAMILY_TREES, getGuestById, getGuestsByGroup, getGuestsByCity, searchGuests, getSocialNodes, getSocialEdges }.

    Create narsh2026/our-people/our-people.css following the our-story.css analog patterns. Include:
    - File header comment: /* Our People -- Page-Specific Styles */
    - Skip link (.skip-to-graph) with same pattern as .skip-map (position absolute, top -100px, focus brings to top)
    - Graph page layout (.graph-page): no max-width constraint (graph needs full width), padding var(--space-md)
    - Graph controls container (.graph-controls): display flex, gap var(--space-md), align-items center, flex-wrap wrap
    - Search input (.graph-search): flex 1, max-width 320px, min-height 44px, padding var(--space-sm) var(--space-md), border 1px var(--color-dusty-rose), background var(--color-warm-white), border-radius var(--radius-sm), font-size 16px (prevents iOS zoom), font-family var(--font-body). Focus state: 2px var(--color-terracotta) border, var(--shadow-soft).
    - View toggle (.view-toggle): display flex, border-radius var(--radius-sm), overflow hidden. View buttons (.view-btn): min-width 100px, height 36px, border 1px var(--color-dusty-rose), background var(--color-warm-white), color var(--color-text-primary), font-size var(--font-size-sm), font-weight 600, font-family var(--font-body), cursor pointer. Active state (.view-btn.active): background var(--color-terracotta), color white, border-color var(--color-terracotta).
    - Filter bar (.filter-bar): display flex, gap var(--space-sm), overflow-x auto, padding-bottom var(--space-xs). Filter buttons (.filter-btn): white-space nowrap, padding var(--space-sm) var(--space-md), min-height 44px, border 1px var(--color-dusty-rose), background var(--color-warm-white), border-radius var(--radius-sm), font-size var(--font-size-sm), font-family var(--font-body), cursor pointer. Active: background var(--color-terracotta), color white, border-color transparent. Hover: background rgba(201, 146, 142, 0.2).
    - Graph canvas container: width 100%, height calc(100vh - 200px), min-height 400px, background var(--color-cream), border-radius var(--radius-md), overflow hidden.
    - Loading state (.graph-loading): centered absolute positioning, var(--color-text-secondary) text, var(--color-cream) background, padding var(--space-md) var(--space-lg), border-radius var(--radius-md).
    - Error state (.graph-error.hidden): display none. .graph-error: same centered positioning as loading.
    - Pinch hint (.pinch-hint): position fixed, bottom var(--space-lg), left 50%, transform translateX(-50%), background rgba(255, 248, 240, 0.9), color var(--color-text-secondary), font-size 16px, padding var(--space-sm) var(--space-md), border-radius var(--radius-sm), opacity 1, transition opacity 0.5s ease. .pinch-hint.hidden: opacity 0, pointer-events none.
    - Search announce (.search-announce): sr-only (position absolute, width 1px, height 1px, overflow hidden, clip rect(0,0,0,0)).
    - SVG node styles: .node cursor pointer. .node-label: font-size 12px, font-weight 600, fill var(--color-text-primary), text-anchor middle, pointer-events none. .tree-node .node-label: font-size 14px.
    - Focus visible on interactive elements: outline 2px solid var(--color-terracotta), outline-offset 2px.
    - Desktop breakpoint @media (min-width: 768px): .graph-controls flex-wrap nowrap, justify-content space-between. .view-btn height 36px. Graph canvas height calc(100vh - 180px).
    - Mobile: .graph-controls flex-direction column. .graph-search max-width 100%. .view-toggle width 100%, justify-content center.
    - Reduced motion @media (prefers-reduced-motion: reduce): disable all transitions on graph elements, set transition to none.

    Modify narsh2026/styles.css :root block: add graph-specific CSS custom properties after the existing Journey Line Colors comment block:
    /* Graph Visualization Tokens */
    --color-node-default: #C9928E;
    --color-node-couple: #C2704F;
    --color-cluster-opacity: 0.08;
    --color-edge-default: rgba(61, 43, 31, 0.15);
    --color-edge-highlight: rgba(61, 43, 31, 0.5);
    --color-node-dim: 0.25;
  </action>
  <verify>
    <automated>grep -c "const NARSH_GUESTS" narsh2026/our-people/guest-data.js && grep -c "GROUPS\|CITIES\|GUESTS\|EDGES\|HOUSEHOLDS\|FAMILY_TREES\|getSocialNodes\|getSocialEdges" narsh2026/our-people/guest-data.js && grep -c ".graph-canvas\|.graph-search\|.filter-btn\|.view-btn\|.graph-loading\|.pinch-hint\|.skip-to-graph" narsh2026/our-people/our-people.css && grep -c "\-\-color-node-default\|\-\-color-node-couple\|\-\-color-cluster-opacity\|\-\-color-edge-default" narsh2026/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - guest-data.js contains "const NARSH_GUESTS = (() =>" and "use strict"
    - guest-data.js exports GROUPS (5 groups), CITIES (7 cities), GUESTS (20-30 entries), EDGES (15-25 entries), HOUSEHOLDS (2-3 entries), FAMILY_TREES (with natalie and arash keys)
    - guest-data.js exports getSocialNodes and getSocialEdges functions
    - guest-data.js GUESTS array includes objects with id "natalie" and "arash" with isCouple: true
    - guest-data.js GUESTS array includes guests with familyTree property containing family, parentId, and generation fields
    - our-people.css contains .graph-canvas, .graph-search, .filter-btn, .view-btn, .graph-loading, .graph-error, .pinch-hint, .skip-to-graph styles
    - our-people.css contains @media (min-width: 768px) desktop breakpoint and @media (prefers-reduced-motion: reduce) block
    - styles.css :root contains --color-node-default, --color-node-couple, --color-cluster-opacity, --color-edge-default, --color-edge-highlight, --color-node-dim
  </acceptance_criteria>
  <done>Guest data module exists with ~25 placeholder guests, typed edges, household groupings, and family tree structures. Page-specific CSS covers all graph UI elements with responsive breakpoints and reduced motion. Shared styles.css has graph visualization tokens.</done>
</task>

<task type="auto">
  <name>Task 2: D3 graph module, UI module, and page shell with full wiring</name>
  <files>narsh2026/our-people/graph.js, narsh2026/our-people/graph-ui.js, narsh2026/our-people/index.html</files>
  <read_first>
    - narsh2026/our-people/guest-data.js (just created in Task 1 -- data contracts for NARSH_GUESTS)
    - narsh2026/our-people/our-people.css (just created in Task 1 -- CSS class names to match)
    - narsh2026/our-story/map.js (analog for IIFE visualization module: init pattern, private state, reduced motion, layer ordering, return object)
    - narsh2026/our-story/scroll-controller.js (analog for UI controller module: init with event listener setup, event handler pattern)
    - narsh2026/our-story/index.html (analog for page shell: head block, body.auth-pending, header/nav, CDN script loading, inline init script)
    - .planning/phases/03-guest-graph/03-RESEARCH.md (D3 patterns: force simulation, photo nodes with clipPath, convex hull clusters, zoom behavior, anti-patterns)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (node sizes, edge styling, cluster region specs, accessibility requirements, interaction states)
    - .planning/phases/03-guest-graph/03-PATTERNS.md (CDN + script loading order, inline init script pattern, header/nav markup)
  </read_first>
  <action>
    Create narsh2026/our-people/graph.js as a NARSH_GRAPH IIFE module following the NARSH_MAP pattern.

    Module-level constants: NODE_RADIUS_DEFAULT = 20, NODE_RADIUS_COUPLE = 28, NODE_RADIUS_TREE = 26, NODE_RADIUS_TREE_COUPLE = 32, EXPANDED_RADIUS = 40, FORCE_CHARGE = -200, FORCE_LINK_DISTANCE = 80, FORCE_COLLIDE_PADDING = 8, ALPHA_REHEAT = 0.3, COLOR_ARASH = "#2A9D8F", COLOR_NATALIE = "#D4A843", COLOR_BOTH = "#C2704F", COLOR_NODE_DEFAULT = "#C9928E", COLOR_EDGE = "rgba(61, 43, 31, 0.15)".

    Private state with let: svgEl = null, simulation = null, currentView = "social", expandedNodeId = null, zoomBehavior = null, innerGroupEl = null, width = 0, height = 0. Also: const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches (check once at module load).

    init(containerId) function:
    1. Select SVG element by id using d3.select, get clientWidth/clientHeight, set viewBox attribute.
    2. Create d3.zoom() with scaleExtent [0.3, 3], on "zoom" handler updates innerGroupEl transform to event.transform. Store as zoomBehavior. Call svgEl.call(zoomBehavior).
    3. Create inner group: svgEl.append("g").attr("class", "graph-inner"). Store as innerGroupEl.
    4. Append layer groups inside innerGroupEl in order: g.cluster-regions (back), g.edges (middle), g.nodes (front).
    5. Call renderSocialGraph().
    6. Hide loading indicator (add "hidden" class to graph-loading element).
    7. Show pinch hint on mobile (check window.innerWidth < 768), auto-hide after 3 seconds or on first touch event.

    renderSocialGraph() function:
    1. Get merged social nodes from NARSH_GUESTS.getSocialNodes(). Set radius property on each: NODE_RADIUS_COUPLE for isCouple, NODE_RADIUS_DEFAULT otherwise. Set initials property (first letter of each word in name, max 2 chars).
    2. Get remapped edges from NARSH_GUESTS.getSocialEdges(socialNodes).
    3. Create SVG defs with clipPath for each node: clipPath id "clip-{nodeId}", circle with node radius.
    4. Create force simulation: d3.forceSimulation(socialNodes) with forceLink(edges).id(d => d.id).distance(FORCE_LINK_DISTANCE), forceManyBody().strength(FORCE_CHARGE), forceCollide().radius(d => d.radius + FORCE_COLLIDE_PADDING), forceX(width/2).strength(0.05), forceY(height/2).strength(0.05). Set alphaDecay(0.03), velocityDecay(0.4).
    5. Render edges as line elements in the edges group: stroke COLOR_EDGE, stroke-width 1 for default, 1.5 for typed edges (type !== "group-link").
    6. Render nodes as g.node elements in the nodes group. Each g gets role="button", tabindex="0", aria-label set to node name. Inside each g: if node has photo, append image with href, width/height 2*radius, x/y -radius, clip-path url(#clip-{id}); else append circle with radius, fill "#FFF8F0", then append text with initials (14px, text-anchor middle, dy 0.35em, fill var(--color-text-secondary)). Append border circle: radius, fill none, stroke COLOR_NODE_DEFAULT (or COLOR_BOTH for isCouple), stroke-width 2 (or 3 for isCouple). Append text.node-label below node: dy radius + 16, text-anchor middle, 12px, font-weight 600, fill #3D2B1F, text is node name.
    7. On simulation tick: update node positions (g transform translate(d.x, d.y)), update edge positions (x1/y1 from source, x2/y2 from target).
    8. Attach click handler on nodes: event.stopPropagation(). This will be wired to expand in Plan 02. For now, log nodeId to console.
    9. Attach click handler on SVG background: collapse any expanded node (Plan 02 will implement).
    10. Call drawClusterRegions() on each tick (throttled to every 10 ticks for performance).

    drawClusterRegions() function:
    1. Clear existing cluster region paths.
    2. For each group in NARSH_GUESTS.GROUPS: filter socialNodes to those whose groups array includes the group id. If fewer than 3 nodes, skip (hull needs 3+ points). Compute hull points from [node.x, node.y]. Call d3.polygonHull(points). If null (collinear), skip. Expand hull outward by 24px padding (iterate each point, move outward from centroid). Draw smoothed path using d3.line().curve(d3.curveCatmullRomClosed.alpha(1)). Set fill to group.color at opacity 0.08, stroke none.

    zoomToNode(nodeId) function: find node by id, compute transform using d3.zoomIdentity.translate(width/2, height/2).scale(1.5).translate(-node.x, -node.y). Apply via svgEl.transition().duration(reducedMotion ? 0 : 500).call(zoomBehavior.transform, transform).

    switchView(view) function: stub for Plan 03. Sets currentView, logs to console.

    filterByGroup(groupId) function: stub for Plan 02. Logs to console.

    Return: { init, switchView, filterByGroup, zoomToNode, expandNode: () => {}, collapseNode: () => {}, getSimulation: () => simulation, getCurrentView: () => currentView }.

    Create narsh2026/our-people/graph-ui.js as a NARSH_GRAPH_UI IIFE module following NARSH_SCROLL pattern.

    Private state: let searchInputEl = null, let filterBarEl = null, let activeFilters = [], let currentView = "social".

    init() function:
    1. Get DOM references with El suffix: searchInputEl = document.getElementById("graph-search"), filterBarEl = document.getElementById("filter-bar"), viewToggleEl = document.getElementById("view-toggle").
    2. Render filter buttons dynamically into filterBarEl from NARSH_GUESTS.GROUPS: "All" button (active by default) plus one button per group. Each button: class "filter-btn", data-group attribute, aria-pressed "false" ("true" for All).
    3. Attach input event listener on searchInputEl (will be implemented in Plan 02).
    4. Attach click event listeners on view toggle buttons (will be implemented in Plan 03).
    5. Attach click event listeners on filter buttons (will be implemented in Plan 02).

    Return: { init }.

    Rewrite narsh2026/our-people/index.html as a complete page shell:
    - Head: charset utf-8, viewport meta, description meta "Narsh 2026", title "Our People -- Narsh 2026", Google Fonts preconnect + link (add wght@600 to Source Sans 3: "Source+Sans+3:wght@400;600"), stylesheet links to /narsh2026/styles.css and /narsh2026/our-people/our-people.css.
    - body class="auth-pending".
    - Skip link: a href="#graph-canvas" class="skip-to-graph" text "Skip to guest graph".
    - Header: exact same site-header markup as our-story/index.html but with class="active" on the Our People nav link.
    - Main with class="graph-page": h1 "Our People". Div.graph-controls containing: input#graph-search type="search" class="graph-search" placeholder="Search for a guest..." aria-label="Search guests"; div#view-toggle.view-toggle role="radiogroup" aria-label="View mode" with two buttons role="radio" -- "Everyone" (aria-checked="true", class="view-btn active", data-view="social") and "Family Tree" (aria-checked="false", class="view-btn", data-view="tree"). Div#filter-bar.filter-bar role="group" aria-label="Filter by group" (empty, populated by JS). Div#graph-loading.graph-loading text "Drawing the connections...". Div#graph-error.graph-error.hidden. SVG#graph-canvas.graph-canvas role="img" aria-label="Interactive guest relationship visualization" aria-describedby="graph-desc" with desc#graph-desc. Div#pinch-hint.pinch-hint text "Pinch to zoom, tap a face to learn more". Div#search-announce.search-announce aria-live="polite".
    - Scripts in order: D3 CDN (https://cdn.jsdelivr.net/npm/d3@7), /narsh2026/auth.js, /narsh2026/nav.js, /narsh2026/our-people/guest-data.js, /narsh2026/our-people/graph.js, /narsh2026/our-people/graph-ui.js.
    - Inline init script: const tier = NARSH_AUTH.requireAuth(); if (tier) { NARSH_AUTH.applyTierVisibility(tier); NARSH_GRAPH.init("graph-canvas"); NARSH_GRAPH_UI.init(); }
  </action>
  <verify>
    <automated>grep -c "const NARSH_GRAPH = " narsh2026/our-people/graph.js && grep -c "d3.forceSimulation\|d3.zoom\|d3.polygonHull\|clipPath" narsh2026/our-people/graph.js && grep -c "const NARSH_GRAPH_UI = " narsh2026/our-people/graph-ui.js && grep -c "cdn.jsdelivr.net/npm/d3@7" narsh2026/our-people/index.html && grep -c "NARSH_GRAPH.init\|NARSH_GRAPH_UI.init" narsh2026/our-people/index.html && grep -c "graph-canvas\|graph-search\|view-toggle\|filter-bar\|graph-loading\|pinch-hint" narsh2026/our-people/index.html</automated>
  </verify>
  <acceptance_criteria>
    - graph.js contains "const NARSH_GRAPH = (() =>" and "use strict"
    - graph.js contains d3.forceSimulation, d3.forceLink, d3.forceManyBody, d3.forceCollide, d3.zoom, d3.polygonHull calls
    - graph.js creates SVG clipPath elements for photo circle nodes
    - graph.js creates g.node elements with role="button" and tabindex="0" and aria-label
    - graph.js handles both photo nodes (image element with clip-path) and initials fallback (circle + text)
    - graph.js couple nodes (isCouple) get larger radius and terracotta border
    - graph.js exports init, switchView, filterByGroup, zoomToNode functions
    - graph-ui.js contains "const NARSH_GRAPH_UI = (() =>" and "use strict"
    - graph-ui.js init() creates filter buttons from NARSH_GUESTS.GROUPS including an "All" button
    - index.html loads D3 v7 from cdn.jsdelivr.net before local scripts
    - index.html has body class="auth-pending" and calls NARSH_AUTH.requireAuth() in inline script
    - index.html has SVG#graph-canvas with role="img" and aria-label
    - index.html has search input, view toggle, filter bar, loading indicator, error div, pinch hint
    - index.html Google Fonts link includes Source+Sans+3:wght@400;600 (600 weight for node labels)
    - Opening the page in a browser after authentication shows a force-directed graph with nodes, edges, and cluster regions
  </acceptance_criteria>
  <done>Force-directed social graph renders on the Our People page with photo-circle nodes (couple emphasized), typed edges, soft colored cluster regions, and zoom/pan navigation. Page shell includes search input, view toggle, and filter bar (functionality stubbed for Plan 02-03). Auth guard and nav are wired. D3 loads from CDN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN to browser | D3.js loaded from external CDN (jsdelivr.net) |
| Guest data to DOM | Guest names, fun facts, and connections inserted into SVG text elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Search input | mitigate | Use D3's .text() method (not .html()) for all DOM insertion. textContent for error messages. Never use innerHTML with user-supplied or data-derived strings. |
| T-03-02 | Information Disclosure | guest-data.js | accept | All guest data is intentionally public to authenticated guests per D-08. No private data (emails, phone numbers) in the data model. Fun facts and connections are editorial content. |
| T-03-03 | Tampering | D3 CDN | accept | jsdelivr.net is a widely-used, reputable CDN. Subresource integrity (SRI) could be added but is deferred since D3 v7 is a stable, well-audited library and the site contains no sensitive operations. |
| T-03-04 | Tampering | SVG image paths | mitigate | Photo paths are hardcoded in guest-data.js (not user-supplied). All paths use root-relative format (/narsh2026/images/people/). No dynamic path construction from user input. |
</threat_model>

<verification>
1. Open narsh2026/our-people/ in browser after authenticating with "fullaccess" password
2. Force-directed graph renders with ~20-30 nodes as photo circles or initials
3. Natalie and Arash nodes are visibly larger with terracotta borders
4. Edges connect related guests with thin lines
5. Soft colored regions appear behind group clusters
6. Scroll-zoom (desktop) and pinch-zoom (mobile) work
7. Pan works by dragging the background
8. Navigation header and auth guard are functional
9. No console errors related to D3 or NARSH_GUESTS
</verification>

<success_criteria>
Guest can authenticate, navigate to Our People, and see an interactive force-directed social graph with photo-circle nodes, edges, cluster regions, and zoom/pan navigation on both desktop and mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guest-graph/03-01-SUMMARY.md`
</output>
