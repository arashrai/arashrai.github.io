---
phase: 03-guest-graph
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - narsh2026/our-people/graph-ui.js
  - narsh2026/our-people/graph.js
  - narsh2026/our-people/our-people.css
  - narsh2026/our-people/index.html
autonomous: true
requirements:
  - GRAPH-03

must_haves:
  truths:
    - "Guest can type a name in the search bar and the graph zooms to the matching node"
    - "Guest can tap a filter button to show only guests in that group"
    - "Multiple group filters can be active simultaneously"
    - "Selecting 'All' deselects specific groups and shows everyone"
    - "Guest can tap/click a node and it expands in place, revealing name, photo, groups, fun fact, and connection info"
    - "When a node is expanded, non-connected nodes dim and connected edges highlight"
    - "Guest can tap the background or press Escape to collapse an expanded node"
    - "On mobile, expanded node details appear as a bottom sheet"
  artifacts:
    - path: "narsh2026/our-people/graph-ui.js"
      provides: "Search input handler, filter toggle logic, expand/collapse orchestration"
      contains: "handleSearch"
    - path: "narsh2026/our-people/graph.js"
      provides: "filterByGroup implementation, expandNode, collapseNode, connection highlighting"
      contains: "expandNode"
    - path: "narsh2026/our-people/our-people.css"
      provides: "Expanded detail card styles, mobile bottom sheet, dimmed node states"
      contains: ".expanded-detail"
  key_links:
    - from: "narsh2026/our-people/graph-ui.js"
      to: "NARSH_GRAPH.filterByGroup"
      via: "filter button click handler"
      pattern: "NARSH_GRAPH\\.filterByGroup"
    - from: "narsh2026/our-people/graph-ui.js"
      to: "NARSH_GRAPH.zoomToNode"
      via: "search match handler"
      pattern: "NARSH_GRAPH\\.zoomToNode"
    - from: "narsh2026/our-people/graph-ui.js"
      to: "NARSH_GRAPH.expandNode"
      via: "node click handler callback"
      pattern: "NARSH_GRAPH\\.expandNode"
---

<objective>
Add search, filtering, and expand-in-place interactions to the social graph. After this plan, guests can search for people by name, filter the graph by relationship group, and tap any node to see detailed information with connected nodes highlighted and non-connected nodes dimmed.

Purpose: Transform the static visualization from Plan 01 into a fully interactive exploration tool.
Output: Updated graph-ui.js with search/filter/expand logic, updated graph.js with filter and expand APIs, updated CSS with detail card and bottom sheet styles.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-guest-graph/03-CONTEXT.md
@.planning/phases/03-guest-graph/03-RESEARCH.md
@.planning/phases/03-guest-graph/03-UI-SPEC.md
@.planning/phases/03-guest-graph/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search and filter functionality</name>
  <files>narsh2026/our-people/graph-ui.js, narsh2026/our-people/graph.js</files>
  <read_first>
    - narsh2026/our-people/graph-ui.js (current state from Plan 01 -- init function, filter button rendering, event listener stubs)
    - narsh2026/our-people/graph.js (current state from Plan 01 -- renderSocialGraph, filterByGroup stub, simulation setup, node rendering)
    - narsh2026/our-people/guest-data.js (NARSH_GUESTS API: searchGuests, getGuestsByGroup, getSocialNodes, getSocialEdges)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (search interaction states, filter button states, empty search results copy)
    - .planning/phases/03-guest-graph/03-RESEARCH.md (Pattern 6 zoom-to-node, anti-pattern about re-creating SVG on filter)
  </read_first>
  <action>
    Update graph-ui.js to implement search and filter interactions:

    Search handler (handleSearch): on input event on searchInputEl, get query value. If empty, clear search state and announce "". If non-empty, call NARSH_GUESTS.searchGuests(query). If results found, call NARSH_GRAPH.zoomToNode(results[0].id) for the first match, and update the search-announce aria-live region with "{N} guest(s) found" using textContent (never innerHTML per T-03-01). If no results, show inline message below search input using the search-announce region: "No guests found for \"{query}\". Try a different name." Debounce the search to 300ms to avoid excessive zoom animations.

    Filter handler (handleFilterClick): when a filter button is clicked, toggle its active state. "All" button is mutually exclusive with specific groups -- selecting "All" deselects all specific groups, selecting a specific group deselects "All". Multiple specific groups CAN be active simultaneously (per UI-SPEC). Update aria-pressed attributes on all buttons. Collect active group ids into activeFilters array. Call NARSH_GRAPH.filterByGroup(activeFilters) where empty array means show all.

    Update graph.js filterByGroup(groupIds) implementation (replace the stub from Plan 01):

    If groupIds is empty or null, show all nodes. Otherwise, filter the social nodes to only those whose groups array includes at least one of the groupIds. Re-run the force simulation with the filtered subset using D3 enter/exit/update pattern (use .join() method per D3 v7 convention -- do NOT destroy and recreate all SVG elements). Filtered-out nodes should fade out over 200ms (or instantly if reducedMotion). Remaining nodes re-settle as the simulation reheats with alpha 0.3. Edges should also be filtered -- only show edges where both source and target are in the visible set. Cluster regions should be redrawn for only the visible groups. Preserve zoom transform state across filter changes.

    Update graph.js zoomToNode(nodeId) implementation: after zooming, apply a single pulse animation on the target node (scale 1.2x then back to 1.0x over 300ms via D3 transition, skip if reducedMotion). The pulse provides visual confirmation of the search result.
  </action>
  <verify>
    <automated>grep -c "handleSearch\|handleFilterClick\|debounce\|searchGuests\|aria-pressed" narsh2026/our-people/graph-ui.js && grep -c "filterByGroup\|activeFilters\|\.join\|alpha(0.3)\|pulse" narsh2026/our-people/graph.js</automated>
  </verify>
  <acceptance_criteria>
    - graph-ui.js contains handleSearch function with debounce (300ms)
    - graph-ui.js uses textContent (not innerHTML) for search announce region per T-03-01
    - graph-ui.js handleFilterClick toggles aria-pressed and active class on filter buttons
    - graph-ui.js "All" button deselects specific groups; specific group deselects "All"
    - graph.js filterByGroup uses D3 .join() for enter/exit/update (not full SVG recreation)
    - graph.js filterByGroup preserves zoom transform state
    - graph.js filterByGroup reheats simulation with alpha(0.3) for filtered subset
    - graph.js zoomToNode applies pulse animation (scale 1.2x -> 1.0x) on target node
    - Typing a guest name in search zooms to matching node with pulse
    - Clicking "College Friends" filter shows only college group nodes and their edges
    - Clicking "All" after filtering restores full graph
  </acceptance_criteria>
  <done>Search bar finds guests by name and zooms to matching nodes with visual pulse. Filter buttons show/hide nodes by group with smooth transitions. Multiple simultaneous group filters supported. All interactions preserve zoom state and use D3 enter/exit/update pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Expand-in-place node interaction with connection highlighting and mobile bottom sheet</name>
  <files>narsh2026/our-people/graph.js, narsh2026/our-people/graph-ui.js, narsh2026/our-people/our-people.css, narsh2026/our-people/index.html</files>
  <read_first>
    - narsh2026/our-people/graph.js (current state including node click handler from Plan 01, simulation reference, node/edge rendering)
    - narsh2026/our-people/graph-ui.js (current state from Task 1 with search/filter handlers)
    - narsh2026/our-people/our-people.css (current state from Plan 01)
    - narsh2026/our-people/index.html (current state from Plan 01)
    - narsh2026/our-people/guest-data.js (guest object fields: name, photo, groups, funFact, connectionToCouple)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (expanded detail card fields, connection highlighting behavior, mobile bottom sheet specs, close interactions)
    - .planning/phases/03-guest-graph/03-RESEARCH.md (Pattern 5 expand-in-place, collision radius increase, alpha reheat)
  </read_first>
  <action>
    Update graph.js expandNode(nodeId) implementation (replace the stub):

    1. If another node is already expanded, collapse it first.
    2. Set expandedNodeId = nodeId. Grow the target node radius to EXPANDED_RADIUS (40px for regular, 48px for couple).
    3. Increase collision force radius for the expanded node to expandedRadius + 20, other nodes stay at their default + FORCE_COLLIDE_PADDING. Reheat simulation with alpha(ALPHA_REHEAT).
    4. Update the clipPath circle and image/circle element sizes with a D3 transition (300ms ease-out, or instant if reducedMotion).
    5. Connection highlighting: build a Set of connected node ids by iterating edges where source or target matches nodeId. Transition non-connected nodes to opacity 0.25 over 200ms. Transition non-connected edges to opacity 0.05. Transition connected edges to stroke "rgba(61, 43, 31, 0.5)" and stroke-width 2. Make connected node labels semibold (font-weight 700). Show edge type labels on highlighted edges: append text.edge-label at edge midpoint, 11px Source Sans 3, color #6B4F3A, text is edge type string.
    6. Show detail card: On desktop (window.innerWidth >= 768), append a foreignObject to the node group containing a div.expanded-detail with the guest's name (17px semibold), groups (14px, comma-separated), connectionToCouple (14px, only if non-null, prefixed with "How we know them"), funFact (14px, only if non-null, prefixed with "Fun fact"). Use textContent for all text insertion (per T-03-01). On mobile (< 768), show a bottom sheet instead -- see graph-ui.js update below.
    7. Publish expanded node data to graph-ui.js via a callback registered during init (onNodeExpand callback).

    Update graph.js collapseNode() implementation:
    1. Reset expanded node radius to original size with transition (200ms ease-in).
    2. Reset collision radius. Reheat simulation with alpha(0.1) for gentle re-settle.
    3. Restore all nodes to opacity 1, all edges to default opacity (0.15).
    4. Remove edge type labels. Remove detail foreignObject.
    5. Set expandedNodeId = null.
    6. Publish collapse event via callback.

    Add keyboard handler in graph.js: on keydown "Escape", call collapseNode(). Attach to document when a node is expanded, remove when collapsed.

    Add background click handler in graph.js (on SVG element click, if target is SVG or inner group, call collapseNode).

    Update the node click handler from Plan 01: replace console.log with actual expandNode call.

    Update graph-ui.js: add onNodeExpand and onNodeCollapse handlers.

    onNodeExpand(guestData): on mobile (window.innerWidth < 768), create or show a bottom sheet div.bottom-sheet at the bottom of the viewport. The bottom sheet contains: close button (X, 44x44px, top-right), guest photo (64px circle) or initials, name (17px semibold), groups (14px), connectionToCouple and funFact (14px each, only if present). Add class "open" to bottom sheet for slide-up animation. Focus trap inside the bottom sheet when open. Set aria-expanded="true" on the originating node.

    onNodeCollapse(): hide the bottom sheet with slide-down animation. Remove focus trap. Return focus to the previously expanded node. Set aria-expanded="false".

    Add the bottom sheet HTML to index.html: a hidden div#bottom-sheet.bottom-sheet with aria-role="dialog" and aria-label="Guest details" containing the close button and content placeholder divs.

    Update our-people.css: add styles for expanded-detail (inline card on desktop), bottom-sheet (mobile slide-up panel), and dimmed/highlighted states.

    Expanded detail card (.expanded-detail): background var(--color-warm-white), border-radius var(--radius-md), box-shadow var(--shadow-medium), padding var(--space-md), max-width 200px. Position relative to the node (via foreignObject positioning).

    Bottom sheet (.bottom-sheet): position fixed, bottom 0, left 0, right 0, max-height 40vh, background var(--color-warm-white), border-radius var(--radius-md) var(--radius-md) 0 0, box-shadow var(--shadow-medium), padding var(--space-lg), transform translateY(100%), transition transform 300ms ease-out, z-index 50. .bottom-sheet.open: transform translateY(0). Close button (.bottom-sheet-close): position absolute, top var(--space-sm), right var(--space-sm), width 44px, height 44px, background transparent, border none, font-size 20px, cursor pointer, display flex, align-items center, justify-content center.

    Dimmed node CSS: .node.dimmed opacity 0.25, transition opacity 200ms. Edge labels (.edge-label): font-size 11px, fill var(--color-text-secondary), pointer-events none.
  </action>
  <verify>
    <automated>grep -c "expandNode\|collapseNode\|expandedNodeId\|connectedIds\|bottom-sheet\|foreignObject\|edge-label" narsh2026/our-people/graph.js && grep -c "onNodeExpand\|onNodeCollapse\|bottom-sheet\|focus-trap\|aria-expanded" narsh2026/our-people/graph-ui.js && grep -c ".expanded-detail\|.bottom-sheet\|.bottom-sheet.open\|.bottom-sheet-close\|.edge-label\|.dimmed" narsh2026/our-people/our-people.css && grep -c "bottom-sheet" narsh2026/our-people/index.html</automated>
  </verify>
  <acceptance_criteria>
    - graph.js expandNode grows node radius and updates clipPath/image/circle sizes with transition
    - graph.js expandNode builds connectedIds Set and dims non-connected nodes to 0.25 opacity
    - graph.js expandNode highlights connected edges (0.5 opacity, 2px width) and shows edge type labels
    - graph.js expandNode creates foreignObject detail card on desktop with name, groups, funFact (if present), connectionToCouple (if present)
    - graph.js collapseNode restores all node/edge opacities and removes detail card and edge labels
    - graph.js Escape keydown calls collapseNode
    - graph.js SVG background click calls collapseNode
    - graph-ui.js onNodeExpand creates bottom sheet on mobile with photo, name, groups, optional fields
    - graph-ui.js onNodeCollapse hides bottom sheet and returns focus to node
    - index.html contains div#bottom-sheet with role="dialog"
    - our-people.css contains .expanded-detail, .bottom-sheet, .bottom-sheet.open, .bottom-sheet-close styles
    - All text insertion uses textContent or D3 .text() method (never innerHTML)
    - Tapping a node expands it with detail card (desktop) or bottom sheet (mobile)
    - Tapping background or pressing Escape collapses the expanded node
  </acceptance_criteria>
  <done>Nodes expand in place revealing detail cards (desktop) or bottom sheets (mobile). Connection highlighting dims non-connected nodes and edges. Edge type labels appear on highlighted edges. Close via background click or Escape key. All interactions are keyboard-accessible with focus management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest data to DOM | Guest names, fun facts, and connections rendered in detail cards and bottom sheets |
| Search input to graph API | User-typed search query passed to searchGuests and displayed in announce region |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Expanded detail card | mitigate | All guest data fields (name, funFact, connectionToCouple, groups) rendered via textContent or D3 .text() method. foreignObject content built with createElement + textContent, never innerHTML. |
| T-03-06 | Tampering | Search input display | mitigate | Search query echoed in "No guests found for {query}" uses textContent on the announce div. Query is never interpolated into HTML strings. |
| T-03-07 | Denial of Service | Rapid search input | mitigate | Search handler debounced to 300ms. Zoom animation queued, not stacked. Prevents rapid-fire D3 transitions from overwhelming the browser. |
</threat_model>

<verification>
1. Type a guest name in search -- graph zooms to matching node with pulse animation
2. Type non-existent name -- "No guests found" message appears below search
3. Click "College Friends" filter -- only college group visible, others fade out
4. Click "All" -- full graph restored
5. Select multiple filters simultaneously -- intersection of groups shown
6. Tap a node -- expands with detail card (desktop) or bottom sheet (mobile)
7. Expanded node shows name, groups, and optional fun fact / connection to couple
8. Non-connected nodes dim to 25% opacity, connected edges highlight
9. Edge type labels appear on highlighted edges
10. Click background or press Escape -- node collapses, all restored
11. On mobile, bottom sheet slides up with close button
</verification>

<success_criteria>
Guest can search for people by name, filter by relationship group, and tap any node to see detailed information with connection highlighting. All interactions work on both desktop and mobile with keyboard accessibility.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guest-graph/03-02-SUMMARY.md`
</output>
