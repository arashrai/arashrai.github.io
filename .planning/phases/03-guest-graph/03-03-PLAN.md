---
phase: 03-guest-graph
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - narsh2026/our-people/graph.js
  - narsh2026/our-people/graph-ui.js
  - narsh2026/our-people/our-people.css
autonomous: true
requirements:
  - GRAPH-02

must_haves:
  truths:
    - "Guest can switch from 'Everyone' (social graph) to 'Family Tree' view using the view toggle"
    - "Family tree shows both Natalie's and Arash's families side by side, connected at the couple"
    - "Family tree nodes are larger than social graph nodes and always show name + photo"
    - "Even couples who are combined household nodes in the social graph appear as individual nodes in the family tree"
    - "Family filter buttons appear when in tree view: 'Both Families', 'Natalie's Family', 'Arash's Family'"
    - "Tree branch lines use family-specific colors (teal for Arash, gold for Natalie)"
    - "Switching views crossfades between layouts over 400ms"
    - "Search still works in tree view (zooms to family tree node)"
  artifacts:
    - path: "narsh2026/our-people/graph.js"
      provides: "renderFamilyTree function, switchView implementation, tree layout with d3.hierarchy and d3.tree"
      contains: "renderFamilyTree"
    - path: "narsh2026/our-people/graph-ui.js"
      provides: "View toggle handler, family tree filter buttons, filter bar swapping"
      contains: "handleViewSwitch"
    - path: "narsh2026/our-people/our-people.css"
      provides: "Tree-specific styles, crossfade transition, family filter buttons"
      contains: ".tree-branch"
  key_links:
    - from: "narsh2026/our-people/graph-ui.js"
      to: "NARSH_GRAPH.switchView"
      via: "view toggle click handler"
      pattern: "NARSH_GRAPH\\.switchView"
    - from: "narsh2026/our-people/graph.js"
      to: "NARSH_GUESTS.FAMILY_TREES"
      via: "reads family tree data for d3.hierarchy"
      pattern: "NARSH_GUESTS\\.FAMILY_TREES"
---

<objective>
Add the family tree layout view and view switching to the guest graph page. After this plan, guests can toggle between the social graph ("Everyone") and a hierarchical family tree ("Family Tree") showing both families side by side connected at the couple, with family-specific filter buttons and crossfade transitions between views.

Purpose: Deliver the second major visualization mode (GRAPH-02) that lets guests explore family relationships in a structured tree layout.
Output: Updated graph.js with tree rendering, updated graph-ui.js with view toggle logic, updated CSS with tree-specific styles.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-guest-graph/03-CONTEXT.md
@.planning/phases/03-guest-graph/03-RESEARCH.md
@.planning/phases/03-guest-graph/03-UI-SPEC.md
@.planning/phases/03-guest-graph/03-01-SUMMARY.md
@.planning/phases/03-guest-graph/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Family tree layout rendering and view switching</name>
  <files>narsh2026/our-people/graph.js, narsh2026/our-people/graph-ui.js</files>
  <read_first>
    - narsh2026/our-people/graph.js (current state from Plan 02 -- renderSocialGraph, expandNode, collapseNode, filterByGroup, simulation, SVG layer groups, zoom behavior, all private state)
    - narsh2026/our-people/graph-ui.js (current state from Plan 02 -- init, handleSearch, handleFilterClick, onNodeExpand, onNodeCollapse, filter button rendering, search handler)
    - narsh2026/our-people/guest-data.js (NARSH_GUESTS.FAMILY_TREES structure: { natalie: {id, name, children: [...]}, arash: {id, name, children: [...]} }, individual guest objects with familyTree property)
    - .planning/phases/03-guest-graph/03-RESEARCH.md (Pattern 3 D3 tree layout, d3.hierarchy, d3.tree, d3.linkVertical, two separate trees positioned side by side, couple connector)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (tree node sizes 52px/64px, tree branch colors, view toggle states, family filter buttons, crossfade transition spec, family tree layout diagram)
    - .planning/phases/03-guest-graph/03-CONTEXT.md (D-14 both families side by side connected at couple, D-15 tree depth as deep as data, D-16 richer/bigger tree nodes, D-17 individual nodes in tree)
  </read_first>
  <action>
    Update graph.js switchView(view) implementation (replace the stub):

    1. If view equals currentView, return early.
    2. Collapse any expanded node first.
    3. Set currentView = view.
    4. Crossfade transition: fade out current content in innerGroupEl over 200ms (opacity 0), then clear SVG content (remove all child elements of cluster-regions, edges, nodes groups), then render the new view, then fade in over 200ms (opacity 1). If reducedMotion, skip transitions (instant swap). Total crossfade: 400ms (200ms out + 200ms in per UI-SPEC).
    5. If view is "social": call renderSocialGraph(). If view is "tree": call renderFamilyTree(). If a family filter is active (from graph-ui.js), pass the family filter value.
    6. Preserve zoom transform state across view switches.

    Add renderFamilyTree(familyFilter) function to graph.js:

    1. Stop and clear the force simulation (simulation.stop()).
    2. Get family tree data from NARSH_GUESTS.FAMILY_TREES. Build d3.hierarchy for each family: natalieRoot = d3.hierarchy(NARSH_GUESTS.FAMILY_TREES.natalie), arashRoot = d3.hierarchy(NARSH_GUESTS.FAMILY_TREES.arash).
    3. Create tree layout: d3.tree().nodeSize([100, 140]) (100px horizontal spacing between siblings, 140px vertical spacing between generations).
    4. Apply layout to each root: treeLayout(natalieRoot), treeLayout(arashRoot).
    5. If familyFilter is "natalie", only render natalieRoot. If "arash", only render arashRoot. If "both" or null/undefined, render both.
    6. When rendering both: offset arashRoot nodes so the two trees sit side by side. Calculate the width of natalieRoot's tree from its node positions. Shift all arashRoot node x positions by natalieWidth + 120px gap. Center both trees in the viewport by computing the combined width and offsetting.
    7. Draw couple connector line: a horizontal path from Natalie's node to Arash's node at their y position, stroke var(--color-terracotta), stroke-width 3, stroke-dasharray "6 4" for a subtle dashed line.
    8. Draw tree branch lines using d3.linkVertical().x(d => d.x).y(d => d.y). Branch lines from natalieRoot: stroke "#D4A843" (gold), from arashRoot: stroke "#2A9D8F" (teal). Stroke-width 2, fill none. Append as path.tree-branch elements in the edges group.
    9. Render tree nodes as g.tree-node elements in the nodes group. Use NODE_RADIUS_TREE (26px) for regular, NODE_RADIUS_TREE_COUPLE (32px) for couple nodes (per D-16 bigger tree nodes). Each tree node always shows name + photo (or initials fallback) -- name label always visible, not just on hover. Node styling follows same photo-circle pattern as social graph but with larger radius. Use individual guest data from NARSH_GUESTS (look up by id from tree node data) for photo, name, initials.
    10. Per D-17: even if a guest is part of a household in the social graph, they appear as an individual node in the family tree. Use the original guest data, not household-merged data.
    11. Tree nodes are clickable (expand-in-place same as social graph). Attach same click handler. In tree view, expanded node detail card appears below the node (not push outward since tree positions are static).
    12. Set SVG desc element text: "{N} family members shown in family tree view".
    13. If reducedMotion, render final positions without animation.

    Update graph.js zoomToNode to work in both views: in social view, find node by id in simulation.nodes(). In tree view, find node by id in the rendered tree node data. Apply same zoom transform.

    Update graph-ui.js to handle view toggle:

    handleViewSwitch(view) function: when a view-toggle button is clicked, update active class and aria-checked on both buttons. Call NARSH_GRAPH.switchView(view). Swap the filter bar contents: in social view, show relationship group filters (NARSH_GUESTS.GROUPS). In tree view, show family filters: "Both Families" (default, active), "Natalie's Family", "Arash's Family". The family filter buttons use data-family attribute ("both", "natalie", "arash") instead of data-group.

    handleFamilyFilterClick(family) function: when a family filter button is clicked in tree view, update active states. Call NARSH_GRAPH.switchView("tree") passing the family filter, or add a new NARSH_GRAPH.filterFamilyTree(family) function to re-render the tree with only the selected family.

    Add filterFamilyTree(family) to graph.js return object: clears tree SVG content and calls renderFamilyTree(family). "both" shows both trees, "natalie" shows only Natalie's tree, "arash" shows only Arash's tree. Center the single tree in the viewport when filtering to one family.

    Wire the view toggle button click listeners in init() (replacing the stubs from Plan 01).
  </action>
  <verify>
    <automated>grep -c "renderFamilyTree\|switchView\|d3.hierarchy\|d3.tree\|d3.linkVertical\|tree-branch\|tree-node\|filterFamilyTree\|couple-connector" narsh2026/our-people/graph.js && grep -c "handleViewSwitch\|handleFamilyFilterClick\|data-family\|Both Families\|Natalie.*Family\|Arash.*Family" narsh2026/our-people/graph-ui.js</automated>
  </verify>
  <acceptance_criteria>
    - graph.js contains renderFamilyTree function that uses d3.hierarchy and d3.tree
    - graph.js renderFamilyTree renders two trees side by side with offset computation
    - graph.js renderFamilyTree draws couple connector path between Natalie and Arash nodes
    - graph.js renderFamilyTree draws tree-branch paths with family-specific colors (teal for arash, gold for natalie)
    - graph.js renderFamilyTree uses NODE_RADIUS_TREE (26px) and NODE_RADIUS_TREE_COUPLE (32px) for larger tree nodes per D-16
    - graph.js renderFamilyTree renders individual nodes even for household members per D-17
    - graph.js switchView crossfades between social and tree views (400ms total or instant if reducedMotion)
    - graph.js switchView preserves zoom transform state
    - graph.js filterFamilyTree shows only the selected family's tree or both
    - graph-ui.js handleViewSwitch updates toggle button active states and aria-checked
    - graph-ui.js swaps filter bar between group filters (social) and family filters (tree)
    - graph-ui.js family filter buttons: "Both Families", "Natalie's Family", "Arash's Family"
    - Clicking "Family Tree" toggle shows hierarchical tree with both families
    - Clicking "Natalie's Family" filter shows only her family tree centered
    - Clicking "Everyone" toggle returns to the social graph
    - Search works in both views -- typing a family member name zooms to their tree node
  </acceptance_criteria>
  <done>Family tree view renders both families side by side connected at the couple with family-colored branch lines and larger nodes. View toggle crossfades between social and tree views. Family filter buttons isolate individual family trees. Search and expand-in-place work in both views.</done>
</task>

<task type="auto">
  <name>Task 2: Tree view CSS, crossfade polish, and accessibility refinements</name>
  <files>narsh2026/our-people/our-people.css</files>
  <read_first>
    - narsh2026/our-people/our-people.css (current state from Plan 02 -- all existing styles)
    - narsh2026/our-people/graph.js (current state from Task 1 -- tree-branch, tree-node, couple-connector class names used)
    - .planning/phases/03-guest-graph/03-UI-SPEC.md (tree node sizes, branch styling, crossfade timing, family filter button specs, reduced motion rules)
  </read_first>
  <action>
    Update our-people.css to add tree-view-specific styles:

    Tree branch lines (.tree-branch): stroke-width 2, fill none, stroke-linecap round.

    Couple connector (.couple-connector): stroke-width 3, stroke-dasharray 6 4, fill none.

    Tree nodes (.tree-node): cursor pointer. .tree-node .node-label: font-size 14px per D-16 (already defined in Plan 01, verify it is present). .tree-node .node-border: stroke-width 2 (3 for couple).

    Crossfade transition on .graph-inner: transition opacity 200ms ease. Add .graph-inner.fading class that sets opacity to 0.

    Family filter buttons: same styling as group filter buttons (already defined for .filter-btn). No additional CSS needed unless the family filter buttons need distinct styling. Verify the existing .filter-btn styles apply correctly to the swapped filter bar content.

    Focus visible on tree nodes: .tree-node:focus-visible outline 2px solid var(--color-terracotta), outline-offset 2px (same pattern as social nodes).

    Reduced motion additions: in the @media (prefers-reduced-motion: reduce) block, add .graph-inner transition none, .tree-branch and .couple-connector transition none.

    SVG text styling for tree: ensure .tree-node text elements inherit font-family var(--font-body) via CSS (SVG text does not inherit body font by default -- set font-family explicitly on .tree-node .node-label and .tree-node .node-initials).

    Verify the existing expanded-detail and bottom-sheet styles from Plan 02 work correctly for tree node expansion (tree nodes use static positions, so the foreignObject detail card should appear below the node, not be pushed by simulation).
  </action>
  <verify>
    <automated>grep -c ".tree-branch\|.couple-connector\|.tree-node\|graph-inner.fading\|prefers-reduced-motion" narsh2026/our-people/our-people.css</automated>
  </verify>
  <acceptance_criteria>
    - our-people.css contains .tree-branch style with stroke-width 2 and fill none
    - our-people.css contains .couple-connector style with stroke-dasharray
    - our-people.css contains .tree-node .node-label with font-size 14px and font-family var(--font-body)
    - our-people.css contains .graph-inner.fading with opacity 0 for crossfade
    - our-people.css reduced motion block includes .graph-inner transition none
    - our-people.css focus-visible styles apply to .tree-node elements
    - Tree branch lines render with correct stroke-width and rounded caps
    - Couple connector renders as a dashed line
    - Tree node labels are 14px semibold Source Sans 3
    - View switching crossfade is visually smooth (no flash of unstyled content)
  </acceptance_criteria>
  <done>Tree-specific CSS styles complete. Crossfade transition between views is polished. Reduced motion respected. Tree node typography and focus styles match the design system. All tree elements styled consistently with the warm, organic aesthetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guest data to tree layout | Family tree data rendered as SVG elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | Family tree node labels | mitigate | All family member names rendered via D3 .text() method (inherits from Plan 01/02 text rendering pattern). No innerHTML usage in tree rendering. |
| T-03-09 | Information Disclosure | Family tree structure | accept | Family relationship structure (parent-child, generations) is intentionally visible to all authenticated guests per D-08. This is wedding social context, not sensitive private data. |
</threat_model>

<verification>
1. Click "Family Tree" toggle -- tree view renders with both families side by side
2. Natalie's family tree uses gold branch lines, Arash's uses teal
3. Couple connector line (dashed terracotta) connects Natalie and Arash at center
4. Tree nodes are visibly larger than social graph nodes with 14px labels
5. Click "Natalie's Family" filter -- only her tree shown, centered
6. Click "Both Families" -- returns to dual tree view
7. Click "Everyone" toggle -- crossfades back to social graph
8. Search in tree view zooms to family member node
9. Tap a tree node -- detail card appears below the node
10. View switching preserves zoom level
11. Keyboard Tab through tree nodes, Enter to expand, Escape to collapse
</verification>

<success_criteria>
Guest can switch between social graph and family tree views. Family tree displays both families side by side with family-colored branch lines, a couple connector, and larger nodes with always-visible names. Family filters allow isolating individual family trees. All interactions from Plan 02 (search, expand, keyboard) work in both views.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guest-graph/03-03-SUMMARY.md`
</output>
