---
phase: 03-guest-graph
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified: []
autonomous: false
must_haves:
  truths:
    - "All five Phase 3 success criteria confirmed by visual inspection"
    - "Social graph and family tree views both render correctly"
    - "Search, filters, and expand-in-place work on both desktop and mobile"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 guest graph delivery. Confirm all success criteria are met across both visualization modes (social graph and family tree), all interactions (search, filter, expand), and both form factors (desktop and mobile).

Purpose: Ensure the guest graph meets the warm, personality-forward design quality and all locked decisions before marking the phase complete.
Output: Verified phase completion or specific issues to address.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-guest-graph/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete Phase 3 guest graph delivery</name>
  <files></files>
  <what-built>
    Interactive guest relationship visualization on the Our People page with two view modes:
    1. Force-directed social graph with ~25 placeholder guests as photo-circle nodes, typed edges, soft colored cluster regions, zoom/pan
    2. Hierarchical family tree with both families side by side connected at the couple, family-colored branch lines
    Plus: search by name with zoom-to-node, group/city filters, expand-in-place node details with connection highlighting, mobile bottom sheet, view toggle with crossfade transition
  </what-built>
  <action>
    Present the verification checklist to the user and wait for approval. Start the local server if not already running. No code changes in this task -- verification only.
  </action>
  <how-to-verify>
    Start a local server from the project root:
    ```
    cd /Users/nataliefleury/programming/arashrai.github.io && python3 -m http.server 8000
    ```

    Then test the following:

    **1. Social Graph View (GRAPH-01, GRAPH-04)**
    - Authenticate at http://localhost:8000/narsh2026/ with password "fullaccess"
    - Navigate to "Our People" via the nav menu
    - Verify force-directed graph renders with ~25 nodes as photo circles or initials
    - Verify Natalie and Arash nodes are visually larger with terracotta borders (D-09)
    - Verify soft colored regions appear behind group clusters (D-10)
    - Verify edges connect related guests with thin warm-brown lines
    - Scroll-zoom (desktop) or pinch-zoom (mobile) to zoom in/out
    - Pan by dragging the background

    **2. Search and Filters (GRAPH-03)**
    - Type a guest name in the search bar -- graph should zoom to matching node with pulse animation (D-13)
    - Type a non-existent name -- "No guests found for ..." message appears
    - Click a group filter (e.g., "College Friends") -- only that group's nodes visible
    - Click "All" -- full graph restored
    - Select multiple filters simultaneously

    **3. Expand-in-Place (D-11, D-12)**
    - Click/tap a node -- it expands with detail card showing name, groups, and optional fun fact/connection
    - Non-connected nodes dim, connected edges highlight (connection highlighting)
    - Click background or press Escape to collapse
    - On mobile: expanded node shows as bottom sheet at viewport bottom

    **4. Family Tree View (GRAPH-02)**
    - Click "Family Tree" toggle -- tree view appears with crossfade transition
    - Both families displayed side by side connected at the couple (D-14)
    - Tree nodes are larger than social graph nodes with always-visible names (D-16)
    - Branch lines use family colors: teal (Arash) and gold (Natalie)
    - Couple connector line (dashed terracotta) connects the two trees
    - Click "Natalie's Family" filter -- only her tree shown
    - Click "Both Families" -- returns to dual view

    **5. Cross-cutting**
    - Switch back to "Everyone" -- social graph restored
    - Verify warm cream background throughout (D-19)
    - On mobile, verify pinch-zoom, tap-to-expand, bottom sheet, filter scrolling all work
    - Overall vibe: does the page feel warm, personal, and inviting?
  </how-to-verify>
  <verify>Human approval received</verify>
  <done>Human has verified all 5 areas: social graph view, search and filters, expand-in-place, family tree view, and cross-cutting concerns. Phase 3 is complete.</done>
  <resume-signal>Type "approved" to complete Phase 3, or describe specific issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None -- verification only, no code changes.

## STRIDE Threat Register

No new threats -- verification checkpoint.
</threat_model>

<verification>
Human confirms all Phase 3 success criteria:
1. Force-directed graph renders with guest nodes and relationship edges
2. User can switch to family tree layout for immediate family view
3. Filters allow viewing subsets by group (family, city, relationship type)
4. Core family member nodes display profile photos and names
5. Graph is navigable on mobile (pinch-zoom, tap for details)
</verification>

<success_criteria>
Human approves the complete guest graph experience across both views, all interactions, and both form factors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-guest-graph/03-04-SUMMARY.md`
</output>
