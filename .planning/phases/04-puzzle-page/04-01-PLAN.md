---
phase: 04-puzzle-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - narsh2026/puzzles/index.html
  - narsh2026/puzzles/puzzle.css
  - narsh2026/puzzles/puzzle-data.js
  - narsh2026/puzzles/puzzle.js
  - narsh2026/puzzles/puzzle-ui.js
autonomous: true
requirements:
  - PUZZLE-01
user_setup: []

must_haves:
  truths:
    - "User sees a 4x5 grid of 20 character cards with emoji, name, and profession"
    - "User can tap a card and choose Criminal or Innocent in a guess dialog"
    - "Correct guess flips the card to reveal clue text with role label"
    - "Non-deducible guess shows a warning without revealing the answer"
    - "Incorrect deducible guess shakes the card and increments mistakes counter"
    - "All 20 cards flipped means game is complete"
  artifacts:
    - path: "narsh2026/puzzles/index.html"
      provides: "Page shell with auth guard, nav, script loading order"
      contains: "NARSH_PUZZLE.init"
    - path: "narsh2026/puzzles/puzzle-data.js"
      provides: "NARSH_PUZZLE_DATA IIFE with CHARACTERS, HINT_SEQUENCE, PROFESSION_EMOJI, resolveClue"
      contains: "const NARSH_PUZZLE_DATA"
    - path: "narsh2026/puzzles/puzzle.js"
      provides: "NARSH_PUZZLE IIFE with game state machine, guessRole, deducibility check"
      contains: "const NARSH_PUZZLE"
    - path: "narsh2026/puzzles/puzzle-ui.js"
      provides: "NARSH_PUZZLE_UI IIFE with card grid rendering, guess dialog, card flip"
      contains: "const NARSH_PUZZLE_UI"
    - path: "narsh2026/puzzles/puzzle.css"
      provides: "Card grid layout, card flip CSS, card states, guess dialog, stats bar styles"
      contains: ".puzzle-grid"
  key_links:
    - from: "narsh2026/puzzles/puzzle-ui.js"
      to: "narsh2026/puzzles/puzzle.js"
      via: "NARSH_PUZZLE.guessRole(cardIndex, guessedCriminal)"
      pattern: "NARSH_PUZZLE\\.guessRole"
    - from: "narsh2026/puzzles/puzzle.js"
      to: "narsh2026/puzzles/puzzle-data.js"
      via: "NARSH_PUZZLE_DATA.CHARACTERS[cardIndex]"
      pattern: "NARSH_PUZZLE_DATA\\.CHARACTERS"
    - from: "narsh2026/puzzles/puzzle-data.js"
      to: "clue template system"
      via: "resolveClue(hint, characters, speakerIndex)"
      pattern: "resolveClue"
    - from: "narsh2026/puzzles/index.html"
      to: "auth system"
      via: "NARSH_AUTH.requireAuth() + NARSH_PUZZLE.init()"
      pattern: "NARSH_AUTH\\.requireAuth"
---

## Phase Goal

**As a** wedding guest, **I want to** play a Clues by Sam style logic puzzle on the wedding website, **so that** I can enjoy a fun interactive experience while learning about the couple's world.

<objective>
Build the core playable puzzle game: a 4x5 card grid where players guess each character as criminal or innocent, cards flip to reveal logic clues on correct guesses, and non-deducible guesses are blocked with a warning. This is the thinnest end-to-end slice that delivers a complete playable game.

Purpose: After this plan, a real user can load the puzzle page, see all 20 characters, tap cards to guess, watch cards flip with clue text, and complete the puzzle. The game is fully playable with core mechanics.
Output: Five files (index.html rewrite, puzzle.css, puzzle-data.js, puzzle.js, puzzle-ui.js) delivering a working puzzle game.
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-puzzle-page/04-CONTEXT.md
@.planning/phases/04-puzzle-page/04-RESEARCH.md
@.planning/phases/04-puzzle-page/04-PATTERNS.md
@.planning/phases/04-puzzle-page/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From narsh2026/auth.js (public API):
  const NARSH_AUTH = (() => { ... return { getTier, setTier, requireAuth, checkPassword, applyTierVisibility, hashPassword }; })();

From narsh2026/styles.css :root tokens:
  --color-terracotta: #C2704F;
  --color-cream: #FFF8F0;
  --color-warm-white: #FFFDFB;
  --color-text-primary: #3D2B1F;
  --color-text-secondary: #6B4F3A;
  --color-dusty-rose: #C9928E;
  --color-journey-arash: #2A9D8F;
  --font-heading: "Playfair Display", Georgia, serif;
  --font-body: "Source Sans 3", -apple-system, BlinkMacSystemFont, sans-serif;
  --space-xs: 4px; --space-sm: 8px; --space-md: 16px; --space-lg: 24px; --space-xl: 48px;
  --radius-sm: 6px; --radius-md: 12px;
  --shadow-soft: 0 2px 8px rgba(61, 43, 31, 0.08);
  --shadow-medium: 0 4px 16px rgba(61, 43, 31, 0.12);
  --transition-fast: 150ms ease; --transition-normal: 250ms ease;

From narsh2026/puzzles/index.html (existing placeholder):
  Already has: auth-pending body class, site-header with nav, Puzzles active class, auth.js + nav.js scripts, NARSH_AUTH.requireAuth() inline init.
  Missing: puzzle.css link, puzzle-data.js/puzzle.js/puzzle-ui.js scripts, game content sections.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create puzzle data module and core game logic engine</name>
  <files>narsh2026/puzzles/puzzle-data.js, narsh2026/puzzles/puzzle.js</files>
  <read_first>
    narsh2026/our-people/guest-data.js
    narsh2026/our-people/graph.js
    narsh2026/auth.js
    .planning/phases/04-puzzle-page/04-RESEARCH.md
    .planning/phases/04-puzzle-page/04-PATTERNS.md
  </read_first>
  <action>
    Create puzzle-data.js as NARSH_PUZZLE_DATA IIFE (per D-15, D-16) following the NARSH_GUESTS pattern from guest-data.js.

    The module must contain:

    1. CHARACTERS array of 20 objects (per D-03, D-07): each with properties criminal (boolean), profession (string), name (string), gender (string), hint (string with template tokens like #NAME:N, #C:N, #BETWEEN:pair(X,Y)), paths (array of arrays of card indices -- each inner array is a set of prerequisite cards that must be flipped before this card is deducible). Create a self-consistent sample puzzle dataset where: exactly some characters are criminal and the rest innocent, every clue is logically consistent, every non-starting card has at least one valid path, and at least 2-3 starting cards have empty paths (paths: [[]]) so the game has entry points.

    2. HINT_SEQUENCE array (per D-09): each entry has requires (array of card indices that must already be flipped), sources (array of card indices whose clues point to the reveal), reveals (array of card indices newly deducible). The first entry should have requires: [] and reveal the starting cards.

    3. PROFESSION_EMOJI map (per D-04): maps profession strings to emoji characters. Include at least 8 professions: police, cook, detective, doctor, teacher, artist, lawyer, engineer, scientist, nurse.

    4. DAILY_PUZZLES array (per D-10, D-11): five objects with name, url, emoji, description. Exact values from CONTEXT.md D-10: Clues by Sam (cluesbysam.com), Minute Cryptic (minutecryptic.com), Globle (globle-game.com), Wordle (nytimes.com/games/wordle), FoodGuessr (foodguessr.com).

    5. resolveClue(hintText, characters, speakerIndex) function (per D-08): replaces #NAME:N with character name at index N, #C:N with "column " + ((N % 4) + 1), #BETWEEN:pair(X,Y) with "between NameX and NameY". Handle out-of-bounds gracefully by returning the raw token. Self-references (I/me) are already baked into hint text.

    6. Helper functions: getCharacter(index), getEmoji(profession), getTotalCharacters().

    7. DATA_VERSION constant (string, e.g. "1") for localStorage schema evolution detection (per RESEARCH.md Pitfall 3).

    Return object: { CHARACTERS, HINT_SEQUENCE, PROFESSION_EMOJI, DAILY_PUZZLES, DATA_VERSION, resolveClue, getCharacter, getEmoji, getTotalCharacters }.

    Then create puzzle.js as NARSH_PUZZLE IIFE (per D-15) following the NARSH_GRAPH pattern from graph.js for private state and public API structure.

    Private state variables: puzzleData (reference to NARSH_PUZZLE_DATA), flippedCards (Set), mistakes (number), gameComplete (boolean). Note: timer, hintsUsed, colorTags, dimmedCards are deferred to Plan 02.

    The module must implement:

    1. init(data) -- stores puzzleData reference. No localStorage load in this plan (deferred to Plan 02 with D-06).

    2. guessRole(cardIndex, guessedCriminal) -- the core game state machine (per D-01):
       - If flippedCards.has(cardIndex): return { result: "already-flipped" }
       - If gameComplete: return { result: "game-over" }
       - Get character from puzzleData.CHARACTERS[cardIndex]; if null return { result: "invalid" }
       - Check deducibility: character.paths.some(path => path.every(idx => flippedCards.has(idx))). If not deducible, return { result: "not-deducible" } (per RESEARCH.md Pitfall 1 -- do NOT reveal answer)
       - If guessedCriminal === character.criminal: add to flippedCards, resolve clue text via puzzleData.resolveClue(character.hint, puzzleData.CHARACTERS, cardIndex), check if flippedCards.size === puzzleData.getTotalCharacters() for game completion, return { result: "correct", clueText, criminal: character.criminal }
       - Else: increment mistakes, return { result: "incorrect" }

    3. getFlippedCards() -- returns new Set(flippedCards)
    4. getMistakes() -- returns mistakes
    5. isComplete() -- returns gameComplete
    6. getCharacterCount() -- returns puzzleData.getTotalCharacters()
    7. reset() -- clears flippedCards, resets mistakes to 0, sets gameComplete to false

    Callback setters for UI communication (following graph.js pattern): set onCardFlip(cb), set onMistake(cb), set onGameComplete(cb). Call these callbacks from guessRole at appropriate points.

    Return object: { init, guessRole, getFlippedCards, getMistakes, isComplete, getCharacterCount, reset, and the callback setters }.
  </action>
  <verify>
    <automated>node -e "
      // Load puzzle-data.js by evaluating it
      const fs = require('fs');
      const dataCode = fs.readFileSync('narsh2026/puzzles/puzzle-data.js', 'utf8');
      const logicCode = fs.readFileSync('narsh2026/puzzles/puzzle.js', 'utf8');

      // Execute in a mock browser context
      const vm = require('vm');
      const ctx = vm.createContext({ window: {matchMedia: () => ({matches: false})}, document: {addEventListener: () => {}}, console, performance: {now: () => 0}, localStorage: {getItem: () => null, setItem: () => {}} });
      vm.runInContext(dataCode, ctx);
      vm.runInContext(logicCode, ctx);

      const D = ctx.NARSH_PUZZLE_DATA;
      const P = ctx.NARSH_PUZZLE;

      // Data module checks
      console.assert(D.CHARACTERS.length === 20, 'Expected 20 characters');
      console.assert(D.HINT_SEQUENCE.length > 0, 'Expected hint sequence');
      console.assert(typeof D.resolveClue === 'function', 'Expected resolveClue function');
      console.assert(D.DAILY_PUZZLES.length === 5, 'Expected 5 daily puzzles');
      console.assert(typeof D.DATA_VERSION === 'string', 'Expected DATA_VERSION string');

      // Check starting cards exist (at least one card with empty path)
      const starters = D.CHARACTERS.filter(c => c.paths.some(p => p.length === 0));
      console.assert(starters.length >= 2, 'Expected at least 2 starting cards');

      // Logic module checks
      P.init(D);
      // Guess a starter card correctly
      const starter = D.CHARACTERS.findIndex(c => c.paths.some(p => p.length === 0));
      const r1 = P.guessRole(starter, D.CHARACTERS[starter].criminal);
      console.assert(r1.result === 'correct', 'Expected correct guess result');
      console.assert(typeof r1.clueText === 'string', 'Expected clue text string');

      // Guess same card again
      const r2 = P.guessRole(starter, true);
      console.assert(r2.result === 'already-flipped', 'Expected already-flipped');

      // Wrong guess on deducible card
      // Find a non-starter card to test not-deducible
      const nonStarter = D.CHARACTERS.findIndex((c, i) => i !== starter && !c.paths.some(p => p.length === 0));
      if (nonStarter >= 0) {
        const r3 = P.guessRole(nonStarter, true);
        // Should be not-deducible since prerequisites not met
        console.assert(r3.result === 'not-deducible' || r3.result === 'correct' || r3.result === 'incorrect', 'Expected valid result');
      }

      console.log('All puzzle data and logic checks passed');
    " 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - narsh2026/puzzles/puzzle-data.js contains "const NARSH_PUZZLE_DATA = (() =>"
    - CHARACTERS array has exactly 20 objects, each with criminal, profession, name, gender, hint, paths properties
    - At least 2 characters have paths containing an empty array (starting cards)
    - HINT_SEQUENCE array has at least 5 entries, first entry has empty requires array
    - PROFESSION_EMOJI maps at least 8 profession strings to emoji
    - DAILY_PUZZLES has 5 entries with urls matching D-10 (cluesbysam.com, minutecryptic.com, globle-game.com, nytimes.com/games/wordle, foodguessr.com)
    - resolveClue replaces #NAME:N, #C:N, #BETWEEN:pair(X,Y) tokens correctly
    - narsh2026/puzzles/puzzle.js contains "const NARSH_PUZZLE = (() =>"
    - guessRole returns { result: "correct", clueText, criminal } for correct deducible guess
    - guessRole returns { result: "not-deducible" } when no path is fully satisfied
    - guessRole returns { result: "incorrect" } for wrong deducible guess and increments mistakes
    - guessRole returns { result: "already-flipped" } for previously flipped card
    - reset() clears all state (flippedCards empty, mistakes 0, gameComplete false)
  </acceptance_criteria>
  <done>Both IIFE modules load without errors, NARSH_PUZZLE_DATA exports 20 characters with valid paths and 5 daily puzzle links, NARSH_PUZZLE.guessRole correctly handles all four result states (correct, incorrect, not-deducible, already-flipped), and the Node.js verification script passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build page shell, CSS styles, and interactive card grid UI</name>
  <files>narsh2026/puzzles/index.html, narsh2026/puzzles/puzzle.css, narsh2026/puzzles/puzzle-ui.js</files>
  <read_first>
    narsh2026/puzzles/index.html
    narsh2026/puzzles/puzzle-data.js
    narsh2026/puzzles/puzzle.js
    narsh2026/our-people/index.html
    narsh2026/our-people/graph-ui.js
    narsh2026/our-people/our-people.css
    narsh2026/styles.css
    .planning/phases/04-puzzle-page/04-UI-SPEC.md
    .planning/phases/04-puzzle-page/04-PATTERNS.md
  </read_first>
  <action>
    Rewrite narsh2026/puzzles/index.html following the our-people/index.html page shell pattern exactly (per D-14):
    - Preserve body class="auth-pending", site-header with nav (Puzzles link has class="active")
    - Add link to puzzle.css after styles.css
    - Update Google Fonts link to include Source Sans 3 wght@400;600 (need 600 weight for game UI bold elements per UI-SPEC typography)
    - Main content: section.puzzle-game#puzzle-game with aria-label="Clues puzzle game" containing div.puzzle-stats#puzzle-stats and div.puzzle-grid#puzzle-grid
    - Controls bar: div.puzzle-controls#puzzle-controls below the grid
    - Daily puzzles section: section.daily-puzzles#daily-puzzles with h2 "Daily Puzzles We Love" (rendered by JS in Plan 02; just the empty container here)
    - Script loading order: auth.js, nav.js, puzzle-data.js, puzzle.js, puzzle-ui.js
    - Inline init: const tier = NARSH_AUTH.requireAuth(); if (tier) { NARSH_AUTH.applyTierVisibility(tier); NARSH_PUZZLE.init(NARSH_PUZZLE_DATA); NARSH_PUZZLE_UI.init(); }

    Create narsh2026/puzzles/puzzle.css with all styles for the core game (per D-12, D-13, UI-SPEC):

    Phase 4 CSS custom properties in :root scope within puzzle.css:
    --color-criminal: #C2704F (reuses terracotta), --color-innocent: #2A9D8F (reuses journey-arash teal), --color-criminal-light: rgba(194, 112, 79, 0.12), --color-innocent-light: rgba(42, 157, 143, 0.12), --color-card-selected: rgba(212, 168, 67, 0.25), --color-card-dimmed: 0.35, --color-incorrect-flash: #D94F4F.

    Puzzle page layout:
    - .puzzle-page: padding var(--space-md), padding-top var(--space-xl)
    - h1: font-family var(--font-heading), text-align center, font-size 28px (24px below 768px)

    Stats bar (.puzzle-stats):
    - Horizontal flex row, justify-content space-between, max-width 528px, margin 0 auto
    - Background rgba of --color-dusty-rose at 15% opacity, border-radius var(--radius-sm)
    - padding var(--space-sm) var(--space-md), font-size 12px, font-weight 700
    - Three items: timer (left), mistakes (center), hints (right)

    Card grid (.puzzle-grid):
    - CSS Grid: grid-template-columns repeat(4, 1fr), gap var(--space-sm), max-width 528px, margin 0 auto, margin-top var(--space-md)
    - Below 480px: gap var(--space-xs)

    Card (.puzzle-card):
    - perspective: 800px, cursor: pointer, aspect-ratio: 4/5
    - .puzzle-card-inner: position relative, width/height 100%, transition transform 500ms ease, transform-style preserve-3d
    - .puzzle-card.flipped .puzzle-card-inner: transform rotateY(180deg)
    - .puzzle-card-front, .puzzle-card-back: position absolute, inset 0, backface-visibility hidden, -webkit-backface-visibility hidden, border-radius var(--radius-md), padding var(--space-sm), display flex, flex-direction column, align-items center, justify-content center
    - .puzzle-card-front: background var(--color-cream), border 1px solid var(--color-dusty-rose), box-shadow var(--shadow-soft)
    - .puzzle-card-back: transform rotateY(180deg), padding var(--space-sm), text-align left, display flex, flex-direction column
    - .puzzle-card-back.criminal: background var(--color-criminal-light), border-left 3px solid var(--color-criminal)
    - .puzzle-card-back.innocent: background var(--color-innocent-light), border-left 3px solid var(--color-innocent)
    - .puzzle-card:not(.flipped):hover on desktop: box-shadow var(--shadow-medium), transform scale(1.02), transition var(--transition-fast)

    Card content typography:
    - .card-emoji: font-size 28px, line-height 1
    - .card-name: font-size 12px, font-weight 700, max-height 2.4em (2 lines), overflow hidden, text-overflow ellipsis
    - .card-profession: font-size 12px, color var(--color-text-secondary)
    - .role-label: font-size 12px, font-weight 700, text-transform uppercase
    - .role-label.criminal: color var(--color-criminal)
    - .role-label.innocent: color var(--color-innocent)
    - .clue-text: font-size 12px, line-height 1.4, color var(--color-text-primary)

    Guess dialog (.guess-dialog):
    - position absolute, background var(--color-warm-white), box-shadow var(--shadow-medium), border-radius var(--radius-sm), padding var(--space-sm), z-index 10
    - Two buttons side by side: .guess-btn, min-height 44px, border-radius var(--radius-sm), font-size 12px, font-weight 700, cursor pointer, border none
    - .guess-btn.criminal-btn: background var(--color-criminal), color white
    - .guess-btn.innocent-btn: background var(--color-innocent), color white

    Controls bar (.puzzle-controls):
    - flex row, justify-content center, gap var(--space-sm), max-width 528px, margin var(--space-md) auto 0
    - .btn-hint, .btn-inspect, .btn-share: min-height 44px, padding 0 12px, border-radius var(--radius-sm), font-size 12px, font-weight 700, cursor pointer

    Incorrect guess shake animation:
    - @keyframes shake: 0%/100% translateX(0), 25% translateX(-4px), 50% translateX(4px), 75% translateX(-4px)
    - .puzzle-card.shake: animation shake 400ms ease

    Reduced motion:
    - @media (prefers-reduced-motion: reduce): .puzzle-card-inner transition-duration 0s, .puzzle-card.shake animation none, .puzzle-card:hover transform none

    touch-action: manipulation on .puzzle-card (removes 300ms tap delay per RESEARCH.md Pitfall 4)

    Create narsh2026/puzzles/puzzle-ui.js as NARSH_PUZZLE_UI IIFE (per D-15) following the NARSH_GRAPH_UI pattern from graph-ui.js:

    Private state: gridEl, statsEl, controlsEl, activeGuessDialog (reference to currently open dialog), all with El suffix convention.

    const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

    init() function:
    - Look up DOM elements: gridEl = document.getElementById("puzzle-grid"), statsEl = document.getElementById("puzzle-stats"), controlsEl = document.getElementById("puzzle-controls")
    - Call renderGrid() to build the 4x5 card grid
    - Call renderStats() to show initial stats bar (Mistakes: 0 display, timer placeholder showing 0:00, hints placeholder showing Hints: 0)
    - Call renderControls() to add Hint/Inspect/Share buttons (non-functional in this plan, wired in Plan 02)
    - Register callbacks with NARSH_PUZZLE: onCardFlip, onMistake, onGameComplete
    - Add click-outside listener on document to dismiss guess dialog

    renderGrid():
    - For each character index 0-19 from NARSH_PUZZLE_DATA:
      - Create div.puzzle-card with role="button", tabindex="0", aria-label="{name}, {profession}"
      - Create div.puzzle-card-inner containing:
        - div.puzzle-card-front with: span.card-emoji (textContent = NARSH_PUZZLE_DATA.getEmoji(character.profession)), div.card-name (textContent = character.name), div.card-profession (textContent = character.profession)
        - div.puzzle-card-back (initially empty -- populated on flip with role label + clue text)
      - Store data-index attribute on the card
      - Add click event listener: if card not flipped, show guess dialog
      - Add keydown listener: Enter/Space triggers same as click
    - Append all cards to gridEl

    showGuessDialog(cardIndex, cardEl):
    - Dismiss any existing dialog first
    - Create div.guess-dialog positioned relative to the card element
    - Two buttons: "Criminal" (class guess-btn criminal-btn) and "Innocent" (class guess-btn innocent-btn)
    - Criminal button click: call NARSH_PUZZLE.guessRole(cardIndex, true), process result
    - Innocent button click: call NARSH_PUZZLE.guessRole(cardIndex, false), process result
    - Add Escape key listener to dismiss
    - Position dialog above or below card based on viewport space

    processGuessResult(result, cardIndex, cardEl):
    - "correct": add .flipped class to cardEl. Populate card-back: create span.role-label with textContent "CRIMINAL" or "INNOCENT" and appropriate class, create p.clue-text with textContent = result.clueText. Add .criminal or .innocent class to card-back div. Set aria-label to "{name}, {role} -- {clueText}", set aria-disabled="true", remove tabindex. Dismiss guess dialog.
    - "incorrect": add .shake class to cardEl, remove after 400ms. Briefly flash card with incorrect-flash background. Update mistakes display in stats bar. Dismiss guess dialog.
    - "not-deducible": show a simple alert-style message (a temporary div overlay on the card with text "Flip more cards first!" that fades after 2s). In Plan 02 this becomes the full wrong-guess modal. Dismiss guess dialog.
    - "already-flipped": do nothing (should not happen since we check before showing dialog)
    - "game-over": do nothing

    renderStats():
    - Create three spans inside statsEl: timer display ("0:00"), mistakes display ("Mistakes: 0"), hints display ("Hints: 0")
    - Add aria-live="polite" on mistakes and hints spans
    - Timer is static in this plan (functional timer added in Plan 02)

    renderControls():
    - Create three buttons in controlsEl: "Show Hint" (btn-hint, disabled in this plan), "Inspect" (btn-inspect, disabled in this plan), "Share Results" (btn-share, disabled in this plan)
    - All three are styled but non-functional -- Plan 02 wires them

    updateStats():
    - Update mistakes span textContent to "Mistakes: " + NARSH_PUZZLE.getMistakes()

    All text rendering MUST use textContent or createTextNode, NEVER innerHTML (per RESEARCH.md security domain and codebase convention).
    All DOM reference variables use El suffix.
    Null-guard all getElementById calls before use.
    contextmenu event preventDefault on puzzle cards (suppress native context menu for future color tag feature).
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const html = fs.readFileSync('narsh2026/puzzles/index.html', 'utf8');
      const css = fs.readFileSync('narsh2026/puzzles/puzzle.css', 'utf8');
      const ui = fs.readFileSync('narsh2026/puzzles/puzzle-ui.js', 'utf8');

      // HTML checks
      console.assert(html.includes('puzzle.css'), 'Missing puzzle.css link');
      console.assert(html.includes('puzzle-data.js'), 'Missing puzzle-data.js script');
      console.assert(html.includes('puzzle.js'), 'Missing puzzle.js script');
      console.assert(html.includes('puzzle-ui.js'), 'Missing puzzle-ui.js script');
      console.assert(html.includes('NARSH_PUZZLE.init'), 'Missing NARSH_PUZZLE.init call');
      console.assert(html.includes('NARSH_PUZZLE_UI.init'), 'Missing NARSH_PUZZLE_UI.init call');
      console.assert(html.includes('auth-pending'), 'Missing auth-pending body class');
      console.assert(html.includes('puzzle-grid'), 'Missing puzzle-grid container');
      console.assert(html.includes('puzzle-stats'), 'Missing puzzle-stats container');
      console.assert(html.includes('puzzle-controls'), 'Missing puzzle-controls container');
      console.assert(html.includes('daily-puzzles'), 'Missing daily-puzzles container');
      console.assert(html.includes('wght@400;600'), 'Missing Source Sans 3 600 weight');

      // CSS checks
      console.assert(css.includes('.puzzle-grid'), 'Missing .puzzle-grid styles');
      console.assert(css.includes('repeat(4, 1fr)'), 'Missing 4-column grid');
      console.assert(css.includes('perspective'), 'Missing perspective for 3D flip');
      console.assert(css.includes('backface-visibility'), 'Missing backface-visibility');
      console.assert(css.includes('-webkit-backface-visibility'), 'Missing webkit prefix');
      console.assert(css.includes('rotateY(180deg)'), 'Missing rotateY flip');
      console.assert(css.includes('--color-criminal'), 'Missing --color-criminal token');
      console.assert(css.includes('--color-innocent'), 'Missing --color-innocent token');
      console.assert(css.includes('@keyframes shake'), 'Missing shake animation');
      console.assert(css.includes('prefers-reduced-motion'), 'Missing reduced motion query');
      console.assert(css.includes('touch-action'), 'Missing touch-action');

      // UI JS checks
      console.assert(ui.includes('const NARSH_PUZZLE_UI'), 'Missing NARSH_PUZZLE_UI IIFE');
      console.assert(ui.includes('use strict'), 'Missing use strict');
      console.assert(ui.includes('textContent'), 'Should use textContent');
      console.assert(!ui.includes('innerHTML'), 'Must NOT use innerHTML');
      console.assert(ui.includes('guessRole'), 'Missing guessRole call');
      console.assert(ui.includes('puzzle-card-front'), 'Missing card front rendering');
      console.assert(ui.includes('puzzle-card-back'), 'Missing card back rendering');
      console.assert(ui.includes('guess-dialog'), 'Missing guess dialog');
      console.assert(ui.includes('contextmenu'), 'Missing contextmenu handler');
      console.assert(ui.includes('reducedMotion') || ui.includes('reduced-motion'), 'Missing reduced motion check');

      console.log('All page shell, CSS, and UI checks passed');
    " 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - narsh2026/puzzles/index.html loads puzzle.css, puzzle-data.js, puzzle.js, puzzle-ui.js in correct order
    - Google Fonts link includes Source+Sans+3:wght@400;600
    - Page has puzzle-grid, puzzle-stats, puzzle-controls, daily-puzzles containers
    - Inline init calls NARSH_AUTH.requireAuth(), NARSH_PUZZLE.init(NARSH_PUZZLE_DATA), NARSH_PUZZLE_UI.init()
    - puzzle.css defines --color-criminal (#C2704F), --color-innocent (#2A9D8F), --color-criminal-light, --color-innocent-light, --color-card-selected, --color-incorrect-flash
    - puzzle.css has .puzzle-grid with repeat(4, 1fr) and max-width 528px
    - puzzle.css has 3D flip: perspective on .puzzle-card, transform-style preserve-3d on .puzzle-card-inner, backface-visibility hidden (with -webkit- prefix) on both front and back faces
    - puzzle.css has @keyframes shake and @media (prefers-reduced-motion: reduce) overrides
    - puzzle.css has touch-action: manipulation on .puzzle-card
    - puzzle-ui.js contains NARSH_PUZZLE_UI IIFE with "use strict"
    - puzzle-ui.js renders 20 cards with emoji, name, profession using textContent (zero innerHTML usage)
    - puzzle-ui.js creates guess dialog with Criminal and Innocent buttons on card click
    - puzzle-ui.js handles all four guessRole results: correct (flip card, show clue), incorrect (shake, update mistakes), not-deducible (show warning), already-flipped (no-op)
    - All interactive elements have role="button", tabindex="0", and keyboard Enter/Space handling
    - Card cards have aria-label="{name}, {profession}" and flipped cards have aria-disabled="true"
    - Stats bar has aria-live="polite" on changing counters
    - Opening the page in a browser after auth shows a 4x5 grid of character cards that can be tapped to guess
  </acceptance_criteria>
  <done>The puzzle page renders a 4x5 grid of 20 character cards with emoji and names. Tapping a card shows a Criminal/Innocent guess dialog. Correct guesses flip the card with a 3D animation to reveal the role label and clue text. Incorrect guesses shake the card. Non-deducible guesses show a warning. The game is fully playable from start to completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Puzzle data to DOM | Static author-controlled data rendered into DOM elements |
| localStorage read | Previously saved game state parsed from localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage game state | accept | No prizes, no leaderboard. Tampering only affects the player's own experience. Wrap all localStorage reads in try/catch. |
| T-04-02 | Tampering (XSS) | Clue text rendering | mitigate | Use textContent exclusively (never innerHTML) when rendering character names, clue text, and all puzzle data into DOM. Enforced in puzzle-ui.js. |
| T-04-03 | Information Disclosure | Puzzle solution in client JS | accept | Inherent to client-side games. Character criminal/innocent booleans are in the JS source. Obfuscation would add complexity without real security (view-source always works). |
</threat_model>

<verification>
1. Open narsh2026/puzzles/ in browser after authenticating
2. 4x5 grid of 20 character cards visible with emoji faces, names, professions
3. Tap an unflipped card -- guess dialog appears with Criminal and Innocent buttons
4. Tap correct guess -- card flips with 3D animation, back shows role label and clue text
5. Tap incorrect guess on deducible card -- card shakes, mistakes counter updates
6. Tap a card whose prerequisites are not met -- warning message appears, no answer revealed
7. Stats bar shows Mistakes count updating in real time
8. All cards keyboard-navigable with Tab, activatable with Enter/Space
9. Reduced motion: flip and shake animations disabled when OS setting is on
10. Page renders correctly on mobile (4 columns maintained, cards sized appropriately)
</verification>

<success_criteria>
A wedding guest can navigate to the Puzzles page, see 20 character cards in a 4x5 grid, tap cards to guess criminal or innocent, watch correct guesses flip with clue text, and play through to game completion. The core game loop works end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-puzzle-page/04-01-SUMMARY.md`
</output>
