---
phase: 04-puzzle-page
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - narsh2026/puzzles/puzzle.js
  - narsh2026/puzzles/puzzle-ui.js
  - narsh2026/puzzles/puzzle.css
autonomous: true
requirements:
  - PUZZLE-01
  - PUZZLE-02
user_setup: []

must_haves:
  truths:
    - "Player can long-press or right-click cards to cycle through 7 color tags for note-taking"
    - "Player can toggle Inspect mode to dim already-read cards"
    - "Player can press Show Hint to see which cards to flip next"
    - "Timer tracks solve time and pauses when the browser tab is hidden"
    - "Player can share results as an emoji grid copied to clipboard"
    - "Non-deducible guess shows a modal with explanation"
    - "Game completion shows a modal with stats and share/restart options"
    - "Game state persists in localStorage across page refreshes"
    - "Player can start a new game with confirmation dialog"
    - "Daily Puzzles We Love section shows 5 link cards to external puzzles"
  artifacts:
    - path: "narsh2026/puzzles/puzzle.js"
      provides: "Timer, color tags, hint system, localStorage persistence, share results generation"
      contains: "saveState"
    - path: "narsh2026/puzzles/puzzle-ui.js"
      provides: "Color tag UI, inspect mode, hint highlights, modals, daily puzzle links rendering"
      contains: "renderDailyPuzzles"
    - path: "narsh2026/puzzles/puzzle.css"
      provides: "Color tag dots, dimmed state, hint pulse, modal styles, daily puzzle card styles"
      contains: ".daily-puzzle-card"
  key_links:
    - from: "narsh2026/puzzles/puzzle-ui.js"
      to: "narsh2026/puzzles/puzzle.js"
      via: "NARSH_PUZZLE.cycleColorTag(cardIndex)"
      pattern: "cycleColorTag"
    - from: "narsh2026/puzzles/puzzle-ui.js"
      to: "narsh2026/puzzles/puzzle.js"
      via: "NARSH_PUZZLE.getNextHint()"
      pattern: "getNextHint"
    - from: "narsh2026/puzzles/puzzle.js"
      to: "localStorage"
      via: "saveState() / loadState() with narsh-puzzle-state key"
      pattern: "localStorage\\.setItem"
    - from: "narsh2026/puzzles/puzzle-ui.js"
      to: "navigator.clipboard"
      via: "navigator.clipboard.writeText(shareText)"
      pattern: "clipboard\\.writeText"
    - from: "narsh2026/puzzles/puzzle-ui.js"
      to: "narsh2026/puzzles/puzzle-data.js"
      via: "NARSH_PUZZLE_DATA.DAILY_PUZZLES for link rendering"
      pattern: "DAILY_PUZZLES"
---

## Phase Goal

**As a** wedding guest, **I want to** play a Clues by Sam style logic puzzle on the wedding website, **so that** I can enjoy a fun interactive experience while learning about the couple's world.

<objective>
Add the complete feature set to the puzzle game: color tagging for note-taking, inspect/dim mode, progressive hint system, timer with visibility API pause, share results with clipboard, wrong-guess modal, game-complete modal, reset confirmation, localStorage persistence, and the daily puzzle links section. After this plan, the puzzle page delivers the full Clues by Sam experience plus curated daily puzzle links.

Purpose: Transforms the playable core game from Plan 01 into the complete feature-rich experience specified in D-05 and adds the daily puzzle links for PUZZLE-02.
Output: Updated puzzle.js (full state management), updated puzzle-ui.js (all UI features + daily links), updated puzzle.css (all visual states + modals + daily cards).
</objective>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-puzzle-page/04-CONTEXT.md
@.planning/phases/04-puzzle-page/04-RESEARCH.md
@.planning/phases/04-puzzle-page/04-PATTERNS.md
@.planning/phases/04-puzzle-page/04-UI-SPEC.md
@.planning/phases/04-puzzle-page/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan builds on -->

From puzzle-data.js (NARSH_PUZZLE_DATA public API):
  { CHARACTERS, HINT_SEQUENCE, PROFESSION_EMOJI, DAILY_PUZZLES, DATA_VERSION,
    resolveClue, getCharacter, getEmoji, getTotalCharacters }

  HINT_SEQUENCE[i] = { requires: number[], sources: number[], reveals: number[] }
  DAILY_PUZZLES[i] = { name: string, url: string, emoji: string, description: string }
  DATA_VERSION = string (e.g. "1")

From puzzle.js (NARSH_PUZZLE public API, Plan 01 state):
  { init, guessRole, getFlippedCards, getMistakes, isComplete, getCharacterCount, reset,
    set onCardFlip(cb), set onMistake(cb), set onGameComplete(cb) }

  guessRole(cardIndex, guessedCriminal) returns:
    { result: "correct", clueText: string, criminal: boolean }
    { result: "incorrect" }
    { result: "not-deducible" }
    { result: "already-flipped" }
    { result: "game-over" }

From puzzle-ui.js (NARSH_PUZZLE_UI public API, Plan 01 state):
  { init }
  Internal: renderGrid, showGuessDialog, processGuessResult, renderStats, renderControls, updateStats

From puzzle.css (Plan 01 tokens):
  --color-criminal, --color-innocent, --color-criminal-light, --color-innocent-light,
  --color-card-selected, --color-card-dimmed (0.35 opacity), --color-incorrect-flash

From narsh2026/styles.css:
  --color-terracotta: #C2704F; --color-cream: #FFF8F0; --color-warm-white: #FFFDFB;
  --color-dusty-rose: #C9928E; --color-text-primary: #3D2B1F; --color-text-secondary: #6B4F3A;
  --shadow-soft; --shadow-medium; --radius-sm; --radius-md;
  --transition-fast; --transition-normal;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add timer, color tags, hints, localStorage persistence, and share results to game engine</name>
  <files>narsh2026/puzzles/puzzle.js</files>
  <read_first>
    narsh2026/puzzles/puzzle.js
    narsh2026/puzzles/puzzle-data.js
    narsh2026/auth.js
    .planning/phases/04-puzzle-page/04-RESEARCH.md
    .planning/phases/04-puzzle-page/04-CONTEXT.md
  </read_first>
  <action>
    Extend NARSH_PUZZLE IIFE to add the full feature set (per D-05, D-06, D-09).

    Add private state variables:
    - hintsUsed (number, starts 0)
    - currentHintLevel (number, tracks which hint step is active: -1 = no hint shown, 0+ = hint index)
    - colorTags (object, maps cardIndex to color palette index 0-6)
    - dimmedCards (Set, tracks which flipped cards are dimmed in inspect mode)
    - inspectMode (boolean, false)
    - timerStart (number, 0)
    - accumulated (number, 0 -- accumulated milliseconds)
    - timerRunning (boolean, false)

    COLOR_TAG_CYCLE constant array: ["transparent", "#E74C3C", "#E67E22", "#F1C40F", "#27AE60", "#3498DB", "#8E44AD"] (per UI-SPEC color tag palette).

    Timer functions (per D-05, RESEARCH.md Pattern 5):
    - startTimer(): set timerStart = performance.now(), timerRunning = true
    - pauseTimer(): if running, add (performance.now() - timerStart) to accumulated, set timerRunning = false
    - resumeTimer(): if not running and not gameComplete, set timerStart = performance.now(), timerRunning = true
    - getElapsed(): if running return accumulated + (performance.now() - timerStart), else return accumulated
    - formatTime(ms): convert to "M:SS" string (e.g. "2:07")
    - Register document visibilitychange listener: if document.hidden call pauseTimer, else call resumeTimer

    Start the timer on first guessRole call (not on init -- timer should start when player makes first action). Use a firstAction boolean flag.

    Color tag functions (per D-05):
    - cycleColorTag(cardIndex): increment colorTags[cardIndex] mod 7, call saveState, return COLOR_TAG_CYCLE[new index]
    - getColorTag(cardIndex): return COLOR_TAG_CYCLE[colorTags[cardIndex] || 0]

    Inspect/dim mode (per D-05):
    - toggleInspectMode(): flip inspectMode boolean, return inspectMode
    - toggleDimCard(cardIndex): if card is flipped, toggle in dimmedCards Set, call saveState
    - isDimmed(cardIndex): return dimmedCards.has(cardIndex)
    - isInspectMode(): return inspectMode

    Hint system (per D-05, D-09):
    - getNextHint(): find the first HINT_SEQUENCE entry where: all requires are in flippedCards, and at least one reveals card is NOT in flippedCards. Return { sources, reveals } arrays or null if no more hints. Increment hintsUsed if a new hint step is provided (track to avoid double-counting on "Show More"). Set currentHintLevel.
    - getHintHighlights(): return the reveals array from the current hint level, or empty array
    - hideHint(): set currentHintLevel = -1, return empty highlights
    - getHintsUsed(): return hintsUsed

    Share results (per D-05):
    - generateShareText(): Build string per RESEARCH.md code example:
      "Clues Puzzle\n" + stopwatch emoji + " " + formatTime(getElapsed()) + " | " + cross emoji + " " + mistakes + " mistake(s) | " + bulb emoji + " " + hintsUsed + " hint(s)\n\n" + emoji grid.
      Emoji grid: iterate CHARACTERS, red square for criminal, green square for innocent, newline every 4.
    - getShareText(): return generateShareText()

    localStorage persistence (per D-06, RESEARCH.md Pitfall 3):
    - STORAGE_KEY = "narsh-puzzle-state"
    - saveState(): try localStorage.setItem(STORAGE_KEY, JSON.stringify({ version: puzzleData.DATA_VERSION, flipped: Array.from(flippedCards), mistakes, hintsUsed, colorTags, elapsedMs: getElapsed(), dimmedCards: Array.from(dimmedCards), gameComplete })). Catch silently (private browsing).
    - loadState(): try localStorage.getItem(STORAGE_KEY), JSON.parse. If version does not match puzzleData.DATA_VERSION, discard and return null. If parse fails, return null. Otherwise return state object.
    - clearState(): try localStorage.removeItem(STORAGE_KEY). Catch silently.

    Update init(data) to:
    - Store puzzleData reference
    - Call loadState(). If saved state exists and version matches: restore flippedCards from saved.flipped, restore mistakes, hintsUsed, colorTags, dimmedCards, accumulated from elapsedMs, gameComplete. If gameComplete, do NOT start timer.
    - If no saved state, start fresh

    Update guessRole to:
    - Call startTimer on first action (if not already started and not restoring from save)
    - Call saveState after every state change (correct guess, incorrect guess)
    - Stop timer on game completion (pauseTimer, set gameComplete)

    Update reset() to:
    - Clear all state (flippedCards, mistakes, hintsUsed, colorTags, dimmedCards, accumulated, timerRunning, gameComplete, currentHintLevel, firstAction flag)
    - Call clearState()

    Add callback setters: set onColorTagChange(cb), set onInspectToggle(cb), set onHintUpdate(cb)

    Updated return object: { init, guessRole, getFlippedCards, getMistakes, isComplete, getCharacterCount, reset, cycleColorTag, getColorTag, toggleInspectMode, isInspectMode, toggleDimCard, isDimmed, getNextHint, getHintHighlights, hideHint, getHintsUsed, getElapsed, formatTime, getShareText, all callback setters }
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const vm = require('vm');
      const dataCode = fs.readFileSync('narsh2026/puzzles/puzzle-data.js', 'utf8');
      const logicCode = fs.readFileSync('narsh2026/puzzles/puzzle.js', 'utf8');

      let savedState = null;
      const ctx = vm.createContext({
        window: {matchMedia: () => ({matches: false})},
        document: {addEventListener: () => {}, hidden: false},
        console, performance: {now: () => 1000},
        localStorage: {
          getItem: () => savedState,
          setItem: (k, v) => { savedState = v; },
          removeItem: () => { savedState = null; }
        }
      });
      vm.runInContext(dataCode, ctx);
      vm.runInContext(logicCode, ctx);

      const D = ctx.NARSH_PUZZLE_DATA;
      const P = ctx.NARSH_PUZZLE;

      P.init(D);

      // Color tag
      console.assert(typeof P.cycleColorTag === 'function', 'Missing cycleColorTag');
      const tag = P.cycleColorTag(0);
      console.assert(tag === '#E74C3C', 'First cycle should be red, got: ' + tag);

      // Inspect mode
      console.assert(typeof P.toggleInspectMode === 'function', 'Missing toggleInspectMode');
      const inspecting = P.toggleInspectMode();
      console.assert(inspecting === true, 'Expected inspect mode on');

      // Hints
      console.assert(typeof P.getNextHint === 'function', 'Missing getNextHint');
      const hint = P.getNextHint();
      console.assert(hint !== null, 'Expected a hint for starting position');
      console.assert(Array.isArray(hint.reveals), 'Hint should have reveals array');

      // Share text
      console.assert(typeof P.getShareText === 'function', 'Missing getShareText');

      // Timer
      console.assert(typeof P.getElapsed === 'function', 'Missing getElapsed');
      console.assert(typeof P.formatTime === 'function', 'Missing formatTime');
      console.assert(P.formatTime(125000) === '2:05', 'Expected 2:05 for 125000ms, got: ' + P.formatTime(125000));

      // localStorage persistence
      const starter = D.CHARACTERS.findIndex(c => c.paths.some(p => p.length === 0));
      P.guessRole(starter, D.CHARACTERS[starter].criminal);
      console.assert(savedState !== null, 'Expected state saved to localStorage');
      const parsed = JSON.parse(savedState);
      console.assert(parsed.version === D.DATA_VERSION, 'Saved state should include version');
      console.assert(Array.isArray(parsed.flipped), 'Saved state should have flipped array');

      // Reset
      P.reset();
      console.assert(P.getMistakes() === 0, 'Mistakes should be 0 after reset');
      console.assert(P.getFlippedCards().size === 0, 'Flipped cards should be empty after reset');
      console.assert(savedState === null, 'localStorage should be cleared after reset');

      console.log('All Plan 02 puzzle.js feature checks passed');
    " 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - puzzle.js contains cycleColorTag function that cycles through 7 colors (transparent, red, orange, yellow, green, blue, purple)
    - puzzle.js contains toggleInspectMode that returns boolean toggle state
    - puzzle.js contains getNextHint that returns { sources, reveals } from HINT_SEQUENCE
    - puzzle.js contains startTimer/pauseTimer/resumeTimer/getElapsed/formatTime functions
    - puzzle.js registers document visibilitychange listener for timer pause/resume
    - puzzle.js contains generateShareText that produces emoji grid with stats line
    - puzzle.js contains saveState/loadState/clearState with STORAGE_KEY "narsh-puzzle-state"
    - Saved state includes version field matching DATA_VERSION
    - loadState discards saved state if version does not match (schema evolution per RESEARCH.md Pitfall 3)
    - init(data) restores from localStorage if valid saved state exists
    - guessRole calls saveState after every state change
    - reset() clears all state and calls clearState()
    - formatTime(125000) returns "2:05"
  </acceptance_criteria>
  <done>The puzzle game engine supports the complete feature set: timer with visibility pause, color tags (7-color cycle), inspect/dim mode, progressive hints from HINT_SEQUENCE, share results emoji grid, and localStorage persistence with version-based schema evolution. All features are exercised by the Node.js verification script.</done>
</task>

<task type="auto">
  <name>Task 2: Wire full UI features -- modals, hints, color tags, inspect, share, daily puzzle links</name>
  <files>narsh2026/puzzles/puzzle-ui.js, narsh2026/puzzles/puzzle.css</files>
  <read_first>
    narsh2026/puzzles/puzzle-ui.js
    narsh2026/puzzles/puzzle.js
    narsh2026/puzzles/puzzle-data.js
    narsh2026/puzzles/puzzle.css
    narsh2026/puzzles/index.html
    narsh2026/our-people/graph-ui.js
    .planning/phases/04-puzzle-page/04-UI-SPEC.md
    .planning/phases/04-puzzle-page/04-CONTEXT.md
  </read_first>
  <action>
    Extend NARSH_PUZZLE_UI IIFE to add all remaining UI features per D-05 and the daily puzzle links per D-10/D-11. Also extend puzzle.css with styles for all new UI components.

    **puzzle-ui.js changes:**

    Add private state: hintBtnEl, inspectBtnEl, shareBtnEl, timerIntervalId (for updating timer display), hintHighlightedCards (array of card indices currently highlighted).

    Update renderControls() to wire the three control buttons:

    1. Show Hint button (btn-hint):
       - On click: call NARSH_PUZZLE.getNextHint()
       - If hint returned: highlight the reveals cards (add .hint-highlighted class), update button text to "Show More" (if more hints available) or "Hide Hint" (toggle)
       - If currently showing hints and clicked: call NARSH_PUZZLE.hideHint(), remove .hint-highlighted from all cards, button text back to "Show Hint"
       - Three button states: "Show Hint" (default), "Show More" (hint visible, more available), "Hide Hint" (hint visible, toggle off)
       - Add aria-pressed attribute tracking toggle state

    2. Inspect button (btn-inspect):
       - On click: call NARSH_PUZZLE.toggleInspectMode()
       - If inspect mode ON: update button text to "Inspecting", add .active class (terracotta bg, white text), add click handler on flipped cards that toggles .dimmed class via NARSH_PUZZLE.toggleDimCard(cardIndex)
       - If inspect mode OFF: update button text to "Inspect", remove .active class, remove dimmed card click handlers
       - Add aria-pressed attribute

    3. Share Results button (btn-share):
       - On click: get text from NARSH_PUZZLE.getShareText()
       - Try navigator.clipboard.writeText(text)
       - On success: change button text to "Copied!" for 2 seconds then revert (per UI-SPEC copywriting)
       - On failure (catch): show fallback -- change button text to "Copy failed" and create a readonly textarea below the button with the share text pre-selected so user can copy manually (per UI-SPEC and RESEARCH.md Pitfall 6)
       - Always call from within the click event handler (clipboard requires user activation)

    Update renderStats() to show live timer:
    - Start a setInterval (1000ms) that updates the timer span textContent with NARSH_PUZZLE.formatTime(NARSH_PUZZLE.getElapsed())
    - Update timer aria-label every 10 seconds (not every second, per UI-SPEC accessibility)
    - Clear interval on game completion
    - If restoring from saved state, show restored time immediately

    Replace the Plan 01 "not-deducible" inline warning with proper wrong-guess modal (per UI-SPEC):
    - Create div.modal.wrong-guess-modal with div.modal-overlay behind it
    - Modal content: h2 "Not so fast!" (per UI-SPEC copywriting), p "This character can't be logically deduced yet. Flip more cards to gather clues first.", button "Got it" (closes modal)
    - Modal has role="dialog", aria-modal="true", aria-labelledby pointing to the heading
    - Focus trapped inside modal (Tab cycles through focusable elements, per graph-ui.js focus trap pattern)
    - Escape closes modal
    - Click on overlay closes modal
    - Modal enter animation: scale(0.95) + opacity(0) to scale(1) + opacity(1), 250ms ease-out
    - On close, restore focus to the card that triggered the modal (store previousFocusEl)

    Add game-complete modal (per UI-SPEC):
    - Triggered when onGameComplete callback fires
    - Create div.modal.complete-modal
    - Content: h2 "Case Closed!", p with stats "You identified all 20 characters in {time} with {N} mistake(s) and {N} hint(s)." (formatted from NARSH_PUZZLE), emoji grid preview (div.share-preview with the emoji grid text in a pre element using textContent), two buttons: "Share Results" (calls clipboard write) and "Play Again" (triggers reset confirmation)
    - Same modal patterns (role, aria, focus trap, escape, overlay)

    Add reset confirmation modal (per UI-SPEC):
    - Triggered by "Play Again" button or a reset action
    - h2 "Start Over?", p "This will reset your progress. You can't undo this."
    - Two buttons: "Start Over" (destructive, uses --color-incorrect-flash background per UI-SPEC) -- calls NARSH_PUZZLE.reset() then re-renders the grid. "Keep Playing" (outlined) -- dismisses modal.
    - Click on overlay does NOT close this modal (explicit button required per UI-SPEC)

    Add color tag rendering (per D-05, UI-SPEC):
    - On renderGrid, add span.color-tag to each card front (positioned top-right, 8px circle, 4px inset)
    - Desktop: add contextmenu event listener on each card that calls preventDefault then NARSH_PUZZLE.cycleColorTag(cardIndex), update the dot color
    - Mobile: add touchstart listener with 300ms setTimeout for long-press detection. On touchmove or touchend before 300ms, cancel. On successful long-press, cycle color tag. Use a let longPressTimer pattern.
    - Update dot background-color to match the returned color string

    Add dimmed card rendering:
    - When inspect mode is on and a flipped card is clicked, toggle .dimmed class on that card (opacity to --color-card-dimmed value 0.35)
    - On state restore from localStorage, apply .dimmed to all cards in dimmedCards set

    Add speech bubble for correct guesses (per UI-SPEC):
    - On correct guess, after card flip completes (500ms delay), show a div.speech-bubble positioned near the card with the clue text
    - Speech bubble: background --color-warm-white, --radius-sm, 3px left border in role color (criminal/innocent), padding --space-sm, font-size 12px
    - Auto-hide after 3 seconds with fade-out
    - Use textContent for clue text rendering

    Add daily puzzle links rendering (per D-10, D-11, PUZZLE-02):
    - renderDailyPuzzles() function called during init
    - Get dailyPuzzlesEl = document.getElementById("daily-puzzles")
    - Create div.daily-puzzles-grid inside the section
    - For each entry in NARSH_PUZZLE_DATA.DAILY_PUZZLES:
      - Create a.daily-puzzle-card with href, target="_blank", rel="noopener noreferrer"
      - Content: span.daily-emoji (textContent = entry.emoji, 32px font-size), div.daily-info containing div.daily-title (textContent = entry.name, 16px bold) and div.daily-desc (textContent = entry.description, 12px secondary color)
      - Add aria-label="{name} -- opens in new tab"
    - Append grid to dailyPuzzlesEl

    On state restore from localStorage:
    - Apply .flipped class and populate card backs for all previously flipped cards
    - Apply color tag dots for all tagged cards
    - Apply .dimmed for all dimmed cards
    - Update stats bar with restored values
    - If game was complete, show the game-complete modal or update UI to reflect completion

    All text rendering MUST use textContent or createTextNode, NEVER innerHTML.
    All DOM reference variables use El suffix.
    Focus management: store previousFocusEl before opening any modal, restore on close.

    **puzzle.css additions:**

    Color tag dot (.color-tag):
    - position absolute, top 4px, right 4px, width 8px, height 8px, border-radius 50%, pointer-events none
    - Hidden when background is transparent

    Dimmed state (.puzzle-card.dimmed):
    - opacity: var(--color-card-dimmed), transition opacity 200ms ease

    Hint highlight (.puzzle-card.hint-highlighted):
    - @keyframes hintPulse: alternating box-shadow between var(--shadow-soft) and var(--shadow-medium), 600ms ease infinite
    - .puzzle-card.hint-highlighted: animation hintPulse 600ms ease infinite

    Control button states:
    - .btn-hint, .btn-inspect default: border 1px solid var(--color-terracotta), color var(--color-terracotta), background transparent
    - .btn-hint.active, .btn-inspect.active: background var(--color-terracotta), color white
    - .btn-share: background var(--color-terracotta), color white (always filled)
    - .btn-hint:disabled, .btn-inspect:disabled: opacity 0.5, cursor not-allowed

    Modal styles (.modal):
    - position fixed, inset 0, z-index 100, display flex, align-items center, justify-content center
    - .modal-overlay: position absolute, inset 0, background rgba(61, 43, 31, 0.4)
    - .modal-card: position relative, z-index 1, max-width 360px, width calc(100% - 2 * var(--space-lg)), background var(--color-warm-white), border-radius var(--radius-md), padding var(--space-lg), box-shadow var(--shadow-medium)
    - .modal-card h2: font-family var(--font-heading), font-size 20px, margin 0 0 var(--space-md) 0
    - .modal-card p: font-size 16px, line-height 1.5, margin 0 0 var(--space-md) 0
    - .modal-actions: display flex, gap var(--space-sm)
    - .modal-actions button: flex 1, min-height 44px, border-radius var(--radius-sm), font-size 12px, font-weight 700, cursor pointer
    - .modal-btn-primary: background var(--color-terracotta), color white, border none
    - .modal-btn-secondary: background transparent, border 1px solid var(--color-dusty-rose), color var(--color-text-primary)
    - .modal-btn-destructive: background var(--color-incorrect-flash), color white, border none
    - Modal enter: animation modalEnter 250ms ease-out
    - @keyframes modalEnter: from { opacity 0, transform scale(0.95) } to { opacity 1, transform scale(1) }
    - .modal.closing: animation modalExit 150ms ease-in forwards
    - @keyframes modalExit: from { opacity 1, transform scale(1) } to { opacity 0, transform scale(0.95) }

    Speech bubble (.speech-bubble):
    - position absolute, background var(--color-warm-white), border-radius var(--radius-sm), padding var(--space-sm), font-size 12px, box-shadow var(--shadow-soft), z-index 5
    - .speech-bubble.criminal: border-left 3px solid var(--color-criminal)
    - .speech-bubble.innocent: border-left 3px solid var(--color-innocent)
    - .speech-bubble.fade-out: opacity 0, transition opacity 500ms ease

    Daily puzzle link cards:
    - .daily-puzzles: margin-top var(--space-xl), max-width 528px, margin-left auto, margin-right auto
    - .daily-puzzles h2: font-family var(--font-heading), font-size 20px, text-align center, margin-bottom var(--space-md)
    - .daily-puzzles-grid: display grid, grid-template-columns repeat(2, 1fr), gap var(--space-md)
    - Below 768px: grid-template-columns 1fr (single column)
    - .daily-puzzle-card: display flex, align-items center, gap var(--space-sm), padding var(--space-md), background var(--color-warm-white), border-radius var(--radius-md), box-shadow var(--shadow-soft), text-decoration none, color var(--color-text-primary), transition transform var(--transition-fast) and box-shadow var(--transition-fast)
    - .daily-puzzle-card:hover: transform translateY(-2px), box-shadow var(--shadow-medium)
    - .daily-puzzle-card:active: transform translateY(0), box-shadow var(--shadow-soft)
    - .daily-puzzle-card:focus: outline 2px solid var(--color-terracotta), outline-offset 2px
    - .daily-emoji: font-size 32px, flex-shrink 0
    - .daily-title: font-size 16px, font-weight 700
    - .daily-desc: font-size 12px, color var(--color-text-secondary)

    Share preview in game-complete modal:
    - .share-preview: background var(--color-cream), border-radius var(--radius-sm), padding var(--space-sm), font-family monospace, font-size 14px, white-space pre-wrap, margin-bottom var(--space-md)

    Reduced motion additions:
    - @media (prefers-reduced-motion: reduce): .puzzle-card.hint-highlighted animation none, .modal animation-duration 0s, .speech-bubble transition-duration 0s, .daily-puzzle-card:hover transform none

    Focus visible on all new interactive elements:
    - .modal-actions button:focus-visible, .daily-puzzle-card:focus-visible: outline 2px solid var(--color-terracotta), outline-offset 2px
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const ui = fs.readFileSync('narsh2026/puzzles/puzzle-ui.js', 'utf8');
      const css = fs.readFileSync('narsh2026/puzzles/puzzle.css', 'utf8');

      // UI JS feature checks
      console.assert(ui.includes('cycleColorTag'), 'Missing cycleColorTag wiring');
      console.assert(ui.includes('toggleInspectMode'), 'Missing inspect mode wiring');
      console.assert(ui.includes('getNextHint'), 'Missing hint system wiring');
      console.assert(ui.includes('clipboard.writeText') || ui.includes('clipboard'), 'Missing clipboard write');
      console.assert(ui.includes('wrong-guess-modal') || ui.includes('wrongGuess'), 'Missing wrong guess modal');
      console.assert(ui.includes('complete-modal') || ui.includes('completeModal') || ui.includes('gameComplete'), 'Missing game complete modal');
      console.assert(ui.includes('reset-modal') || ui.includes('resetModal') || ui.includes('Start Over'), 'Missing reset confirmation');
      console.assert(ui.includes('daily-puzzle'), 'Missing daily puzzle links rendering');
      console.assert(ui.includes('DAILY_PUZZLES'), 'Missing DAILY_PUZZLES data reference');
      console.assert(ui.includes('target') && ui.includes('_blank'), 'Daily links should open in new tab');
      console.assert(ui.includes('noopener'), 'Daily links should have rel=noopener');
      console.assert(ui.includes('contextmenu'), 'Missing contextmenu for color tags');
      console.assert(ui.includes('touchstart'), 'Missing touchstart for long-press');
      console.assert(ui.includes('aria-modal') || ui.includes('ariaModal'), 'Missing aria-modal on modals');
      console.assert(ui.includes('Escape'), 'Missing Escape key handler for modals');
      console.assert(ui.includes('aria-pressed'), 'Missing aria-pressed on toggle buttons');
      console.assert(ui.includes('speech-bubble') || ui.includes('speechBubble'), 'Missing speech bubble');
      console.assert(!ui.includes('innerHTML'), 'Must NOT use innerHTML');
      console.assert(ui.includes('renderDailyPuzzles') || ui.includes('dailyPuzzle'), 'Missing daily puzzles render function');

      // CSS feature checks
      console.assert(css.includes('.color-tag'), 'Missing color tag dot styles');
      console.assert(css.includes('.dimmed') || css.includes('dimmed'), 'Missing dimmed state');
      console.assert(css.includes('hintPulse') || css.includes('hint-highlighted'), 'Missing hint pulse animation');
      console.assert(css.includes('.modal'), 'Missing modal styles');
      console.assert(css.includes('modal-overlay') || css.includes('modalOverlay'), 'Missing modal overlay');
      console.assert(css.includes('modalEnter') || css.includes('modal-enter'), 'Missing modal enter animation');
      console.assert(css.includes('.speech-bubble'), 'Missing speech bubble styles');
      console.assert(css.includes('.daily-puzzle-card'), 'Missing daily puzzle card styles');
      console.assert(css.includes('.daily-puzzles-grid'), 'Missing daily puzzles grid');
      console.assert(css.includes('repeat(2, 1fr)'), 'Daily links should be 2-column grid');
      console.assert(css.includes('.modal-btn-destructive'), 'Missing destructive button style');
      console.assert(css.includes('.share-preview'), 'Missing share preview style');

      console.log('All Plan 02 UI and CSS feature checks passed');
    " 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - puzzle-ui.js wires Show Hint button: clicking calls getNextHint, highlights reveal cards with .hint-highlighted class, button text cycles through "Show Hint" / "Show More" / "Hide Hint"
    - puzzle-ui.js wires Inspect button: toggles inspect mode, button text changes to "Inspecting" when active, flipped cards become dimmable on click
    - puzzle-ui.js wires Share Results button: copies emoji grid to clipboard, shows "Copied!" for 2 seconds, has fallback textarea on clipboard failure
    - puzzle-ui.js renders wrong-guess modal with "Not so fast!" heading, explanation text, and "Got it" button when non-deducible guess occurs
    - puzzle-ui.js renders game-complete modal with "Case Closed!" heading, stats, emoji grid preview, Share Results and Play Again buttons
    - puzzle-ui.js renders reset confirmation modal with "Start Over?" heading, warning, "Start Over" (destructive) and "Keep Playing" buttons
    - All modals have role="dialog", aria-modal="true", focus trap (Tab cycles), Escape closes (except reset confirmation overlay click)
    - puzzle-ui.js renders color tag dots (8px circles, top-right of card) with right-click cycling (desktop) and long-press cycling (mobile, 300ms)
    - puzzle-ui.js renders daily puzzle links: 5 cards in a 2-column grid (1-column on mobile) with emoji, title, description, opening in new tab with rel="noopener noreferrer"
    - puzzle-ui.js shows speech bubble with clue text after correct guess, auto-hides after 3 seconds
    - puzzle-ui.js updates timer display every second using formatTime
    - puzzle-ui.js restores visual state from localStorage on init (flipped cards, color tags, dimmed cards, stats)
    - Zero innerHTML usage in puzzle-ui.js
    - puzzle.css has styles for .color-tag, .dimmed, .hint-highlighted (pulse animation), .modal, .modal-overlay, .modal-card, .speech-bubble, .daily-puzzle-card, .daily-puzzles-grid, .share-preview
    - puzzle.css has @media (prefers-reduced-motion: reduce) disabling hint pulse, modal animation, speech bubble transition, daily card hover transform
    - puzzle.css has focus-visible outlines on all new interactive elements
    - Daily puzzles grid is 2 columns on desktop (>= 768px), 1 column on mobile
  </acceptance_criteria>
  <done>The puzzle page delivers the complete Clues by Sam experience: color tagging via right-click/long-press, inspect mode to dim read cards, progressive hints highlighting next cards to flip, timer tracking solve time with tab-pause, share results copying emoji grid to clipboard, modal dialogs for wrong guesses / game completion / reset confirmation, speech bubbles on correct guesses, localStorage persistence across refreshes, and a "Daily Puzzles We Love" section with 5 curated external puzzle link cards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Puzzle data to DOM | Static author-controlled data rendered into DOM elements via textContent |
| localStorage to game state | Previously saved JSON parsed and used to restore game state |
| Game state to clipboard | Generated emoji text written to system clipboard |
| External puzzle links | Outbound navigation to third-party puzzle sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | localStorage game state | accept | No prizes, no leaderboard. Tampering only affects the player's own experience. All localStorage reads wrapped in try/catch. Version mismatch discards saved state. |
| T-04-02 | Tampering (XSS) | Clue text rendering | mitigate | Use textContent exclusively in puzzle-ui.js. Zero innerHTML usage enforced by automated verification. |
| T-04-03 | Information Disclosure | Puzzle solution in client JS | accept | Inherent to client-side games. No server-side alternative for a static site. |
| T-04-04 | Information Disclosure | Clipboard write | accept | Only writes pre-formatted emoji text (not user data). Uses navigator.clipboard.writeText (write-only, never reads clipboard). |
| T-04-05 | Spoofing | External puzzle links | mitigate | All external links use rel="noopener noreferrer" to prevent reverse tabnabbing. |
</threat_model>

<verification>
1. Refresh the puzzle page -- previously saved progress restores (flipped cards, stats, color tags)
2. Right-click (desktop) or long-press (mobile) a card -- color tag dot cycles through 7 colors
3. Click "Show Hint" -- suggested cards pulse with shadow animation; click again to show more or hide
4. Click "Inspect" -- button says "Inspecting", clicking flipped cards dims them (opacity 0.35)
5. Tap a non-deducible card and guess -- modal appears with "Not so fast!" explanation
6. Complete the puzzle -- "Case Closed!" modal shows stats and emoji grid
7. Click "Share Results" -- emoji text copied to clipboard, button shows "Copied!" for 2 seconds
8. Click "Play Again" -- confirmation modal asks "Start Over?", confirming resets all state
9. Timer updates every second, pauses when switching browser tabs, resumes on return
10. Scroll below the puzzle -- 5 daily puzzle link cards visible, opening in new tabs
11. Daily puzzle links: Clues by Sam, Minute Cryptic, Globle, Wordle, FoodGuessr
12. All modals close with Escape key, focus returns to the element that triggered the modal
13. Keyboard navigation: Tab through cards and controls, Enter/Space activates
14. Reduced motion: animations disabled when OS preference is set
</verification>

<success_criteria>
The puzzle page delivers the complete Clues by Sam experience with all features from D-05 (color tags, inspect mode, progressive hints, share results, timer, wrong guess warning) plus localStorage persistence and the daily puzzle links section (PUZZLE-02). A wedding guest can play through the entire puzzle, use note-taking tools, share their results, and discover new daily puzzles.
</success_criteria>

<output>
After completion, create `.planning/phases/04-puzzle-page/04-02-SUMMARY.md`
</output>
