---
phase: 260731-pa8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - narsh2026/puzzles/puzzle.css
  - narsh2026/puzzles/puzzle-ui.js
  - narsh2026/puzzles/puzzle.js
  - narsh2026/puzzles/index.html
autonomous: false
requirements: [260731-pa8]

must_haves:
  truths:
    - "Clicking the top-right corner of an unflipped puzzle card cycles its color marker"
    - "Tapping the top-right corner on mobile cycles the color marker exactly once (no double-cycle from long-press)"
    - "Clicking anywhere else on the card still opens the guess dialog / toggles inspect dim"
    - "Right-click and long-press on the card still cycle the color marker"
    - "The card keeps role=button, tabindex=0, and Enter/Space opening the guess dialog"
    - "The corner target does nothing on a flipped card"
    - "A credit line above the puzzle grid links to Clues by Sam and tells the reader a tutorial lives there"
    - "The Clues by Sam link opens in a new tab and announces that fact to screen readers"
    - "The completion grid shows red only for cards the player guessed wrong at least once"
    - "The completion grid shows green for every card solved first-try, including the auto-revealed starter"
    - "A save written before this change still loads, and the share grid renders without error"
  artifacts:
    - path: "narsh2026/puzzles/puzzle.css"
      provides: ".color-tag-hit corner hit target sizing, hover affordance, flipped/reduced-motion guards, .puzzle-credit line styling"
      contains: ".color-tag-hit"
    - path: "narsh2026/puzzles/puzzle-ui.js"
      provides: "Corner hit element creation + click/touchstart handlers in renderGrid"
      contains: "color-tag-hit"
    - path: "narsh2026/puzzles/index.html"
      provides: "Static Clues by Sam credit/tutorial paragraph above the puzzle game section"
      contains: "cluesbysam.com"
    - path: "narsh2026/puzzles/puzzle.js"
      provides: "Per-card mistake tracking (mistakesByCard) + accuracy-based share grid"
      contains: "mistakesByCard"
  key_links:
    - from: "narsh2026/puzzles/puzzle-ui.js (tagHitEl click handler)"
      to: "NARSH_PUZZLE.cycleColorTag"
      via: "direct call, result written to tagDotEl.style.backgroundColor"
      pattern: "cycleColorTag"
    - from: "narsh2026/puzzles/puzzle-ui.js (tagHitEl click handler)"
      to: "cardEl click handler (guess dialog)"
      via: "event.stopPropagation() prevents dialog"
      pattern: "stopPropagation"
    - from: "narsh2026/puzzles/puzzle.css (.color-tag-hit)"
      to: "narsh2026/puzzles/puzzle-ui.js (tagHitEl.className)"
      via: "class name match"
      pattern: "color-tag-hit"
    - from: "narsh2026/puzzles/index.html (.puzzle-credit)"
      to: "narsh2026/puzzles/puzzle.css (.puzzle-credit rules)"
      via: "class name match"
      pattern: "puzzle-credit"
    - from: "narsh2026/puzzles/puzzle.js (guessRole incorrect branch)"
      to: "mistakesByCard"
      via: "per-card increment alongside the global mistakes counter"
      pattern: "mistakesByCard\\[cardIndex\\]"
    - from: "narsh2026/puzzles/puzzle.js (generateShareText)"
      to: "mistakesByCard"
      via: "per-tile red/green decision"
      pattern: "mistakesByCard\\[i\\]"
    - from: "narsh2026/puzzles/puzzle.js (saveState / init / reset)"
      to: "localStorage narsh-puzzle-state"
      via: "mistakesByCard serialized alongside colorTags"
      pattern: "mistakesByCard"
---

<objective>
Three independent improvements to the puzzle page:

1. **Corner color marker** — a ~30px hit target in the top-right of each card so a plain click/tap cycles the color marker, instead of requiring right-click (desktop) or long-press (mobile).
2. **Clues by Sam credit** — a link above the grid crediting Clues by Sam and telling newcomers a tutorial lives there.
3. **Honest completion grid** — the emoji grid in the completion modal / share text currently shows each character's ROLE (criminal vs innocent), which is both a spoiler-flavored non-achievement and not what a share grid is for. Change it to show the PLAYER's accuracy: green for a first-try solve, red for a card they guessed wrong.

Purpose: (1) the color-marker feature is currently undiscoverable and awkward on touch; (2) Clues by Sam is only credited at the very bottom of the page, useless to a guest who is confused by the grid right now; (3) a share grid should say something about how the player did.
Output: `.color-tag-hit` element + CSS wired to `NARSH_PUZZLE.cycleColorTag`; a static `.puzzle-credit` line in `index.html`; new per-card mistake tracking in `puzzle.js` feeding an accuracy-based `generateShareText()`.
</objective>

<scope_note>
Scope grew twice after initial planning. This is now THREE unrelated changes in one plan, which is above the usual 2-3 task budget.

Mitigation: tasks 1-4 are individually atomic and independently committable — nothing in task 3 or 4 depends on task 1 or 2. Commit after each task. If context pressure shows up (executor above ~50% after task 2), stop, commit, `/clear`, and resume at task 3; the tasks share no state.

Suggested commit boundaries:
- Tasks 1+2 -> `feat(puzzle): cycle color marker by clicking the card corner`
- Task 3 -> `feat(puzzle): credit Clues by Sam and point at its tutorial`
- Task 4 -> `fix(puzzle): share grid reflects player accuracy, not character roles`
</scope_note>

<execution_context>
@/Users/nataliefleury/programming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nataliefleury/programming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@narsh2026/puzzles/puzzle-ui.js
@narsh2026/puzzles/puzzle.css
@narsh2026/puzzles/puzzle.js
@narsh2026/puzzles/index.html

<scouted_facts>
Verified by the planner — do not re-derive.

**Card interaction (tasks 1-2)**
- Card DOM (puzzle-ui.js:453-492): `.puzzle-card[role=button][tabindex=0]` > `.puzzle-card-inner` > `.puzzle-card-front` (holds `.color-tag` dot, emoji, name, profession) + `.puzzle-card-back`.
- puzzle-ui.js:468-470 — `<span class="color-tag">` is the FIRST child appended to `.puzzle-card-front`.
- puzzle-ui.js:495-498 — `click` on cardEl -> `event.stopPropagation()` + `handleCardClick(i, cardEl)` (guess dialog / inspect dim).
- puzzle-ui.js:501-507 — `keydown` Enter/Space on cardEl -> `handleCardClick`.
- puzzle-ui.js:509-514 — `contextmenu` on cardEl -> `preventDefault()` + `NARSH_PUZZLE.cycleColorTag(i)` -> writes `tagDotEl.style.backgroundColor`.
- puzzle-ui.js:516-538 — `touchstart` (passive) on cardEl starts a 300ms long-press timer that cycles the tag; `touchmove` / `touchend` clear it.
- puzzle-ui.js:421-428 — restore loop does `cardEl.querySelector(".color-tag")` and writes the persisted color. The `.color-tag` class MUST stay on the dot span, and there must be exactly one `.color-tag` element per card.
- puzzle.js:95-107 — `cycleColorTag(cardIndex)` returns the next color string; `COLOR_TAG_CYCLE[0]` is `"transparent"` (index 0 = cleared).
- puzzle.css:244-257 — `.color-tag` is `position:absolute; top:6px; right:6px; 14px square; border-radius:50%; pointer-events:none;` and transitions `background-color, border-color`.
- puzzle.css:88-105 — 3D flip: `.puzzle-card-inner { transform-style: preserve-3d }`, `.puzzle-card.flipped .puzzle-card-inner { transform: rotateY(180deg) }`, faces have `backface-visibility: hidden`. So on a flipped card the front face is back-facing and browsers already skip it in hit testing — this plan adds an explicit guard anyway rather than relying on that.
- puzzle.css:69-85 — grid is 4 columns, `max-width: 528px`, cards `aspect-ratio: 4/5` -> ~120px wide desktop, ~85px wide on a 390px phone.
- puzzle.css:142-152 — `@media (hover: hover)` block holds desktop-only hover styling.
- puzzle.css:631-660 — `@media (prefers-reduced-motion: reduce)` block neutralizes transforms/animations.
- puzzle.css:673-677 — `@media (max-width: 480px)` block currently only tightens `.puzzle-grid` gap.
- NO user-facing copy anywhere in the repo tells users to right-click. Repo-wide grep for "right-click / right click / long-press / long press" matches ONLY three code comments: `puzzle.js:9`, `puzzle-ui.js:509`, `puzzle-ui.js:516`.

**Clues by Sam credit (task 3)**
- index.html:30-37 — `<main class="puzzle-page">` contains, in order: `<h1>Clues by Narsh</h1>`, `<p class="puzzle-identity-clue" id="puzzle-identity-clue"></p>`, `<section class="puzzle-game">` (stats/grid/controls), `<section class="daily-puzzles">`.
- index.html:48-58 — inline bootstrap script: `NARSH_AUTH.requireAuth()` gates everything; the identity clue `<p>` is populated by JS from `NARSH_PUZZLE_DATA.IDENTITY_CLUE` only after auth passes.
- puzzle-data.js:224 — `{ name: "Clues by Sam", url: "https://cluesbysam.com/", ... }` already exists as the first entry in `DAILY_PUZZLES`, rendered into the bottom "Daily Puzzles We Love" grid. That entry STAYS AS-IS; the new credit line is separate and additional.
- **Existing external-link pattern** (puzzle-ui.js:925-930, `renderDailyPuzzles`): `linkEl.target = "_blank"; linkEl.rel = "noopener noreferrer"; linkEl.setAttribute("aria-label", puzzle.name + " -- opens in new tab");`. House pattern for announcing new-tab links.
- There is NO `.sr-only` / `.visually-hidden` utility anywhere in `puzzles/puzzle.css` or `narsh2026/styles.css` (grep for `sr-only|visually-hidden|clip: rect` returns nothing). The new-tab announcement MUST use the existing `aria-label` pattern rather than an invented hidden span.
- puzzle.css:20-38 — `.puzzle-page h1 { font-family: var(--font-heading); text-align: center; font-size: 28px; margin: 0 0 var(--space-sm) 0; }`; `.puzzle-identity-clue { text-align: center; font-style: italic; color: var(--color-text-secondary); max-width: 32rem; margin: 0 auto var(--space-md) auto; }` plus `.puzzle-identity-clue:empty { display: none; }`.
- Design tokens in `narsh2026/styles.css:5-45`: `--font-size-sm: 14px`, `--font-size-base: 17px`, `--color-terracotta: #C2704F`, `--color-text-primary`, `--color-text-secondary`, `--space-xs/sm/md/lg`, `--radius-sm/md/lg`, `--transition-fast`.
- Existing focus-state house style (puzzle.css:547-550, 625-628): `outline: 2px solid var(--color-terracotta); outline-offset: 2px;`.
- The repo writes em dashes as ASCII `--` in user-facing strings (`<title>Clues by Narsh -- Narsh 2026</title>`, the `-- opens in new tab` aria-labels). Match that.

**Share grid (task 4)**
- puzzle.js:189-210 — `generateShareText()` builds: line 1 `"🌸 Narsh 2026 Puzzle\n"`, line 2 `"⏱️ {time} | ❌ {mistakes} {mistakeWord} | 💡 {hints} {hintWord}\n\n"`, then the grid loop `for i in characters.length` emitting `characters[i].criminal ? "\u{1F7E5}" : "\u{1F7E9}"` with a `\n` inserted every 4 tiles, then `"\n\nCan you crack the case? 🔮\narashrai.com/narsh2026/puzzles/"`. The grid is therefore `shareText.split("\n\n")[1]`.
- puzzle-ui.js:182-187 (`showGameCompleteModal`) writes `NARSH_PUZZLE.getShareText()` into `<pre>` inside `.share-preview`; puzzle-ui.js:246-247 (`copyShareText`) copies the same string for the "Share Results" button. **Confirmed: `generateShareText()` is the single source for the modal preview, the controls-bar Share button, and the clipboard payload.** Fixing it there fixes all three.
- puzzle.js:19 — `let mistakes = 0;` is the ONLY mistake state. puzzle.js:361-368 — the `else` (incorrect) branch of `guessRole` does `mistakes++`, fires `onMistakeCallback(mistakes)`, `saveState()`, returns `{ result: "incorrect" }`. There is NO per-card record. Task 4 must add one.
- Existing per-card persistence patterns to copy: `colorTags` (plain object, puzzle.js:29 / 225 / 268 / 291 / 386) and `dimmedCards` (Set serialized via `Array.from`, puzzle.js:33 / 227 / 269 / 292 / 387). A plain object matches `colorTags` and needs no Set/Array conversion.
- puzzle.js:218-234 `saveState()`, 236-248 `loadState()`, 260-301 `init()`, 381-395 `reset()`, 397-424 module return object.
- **`loadState()` gates on `state.version !== puzzleData.DATA_VERSION` only — never on object shape.** `puzzle-data.js:10` — `const DATA_VERSION = "4";`. Bumping it to "5" makes `loadState()` return null for EVERY existing save.
- puzzle-ui.js:785-818 `autoRevealStarterCard()` calls `NARSH_PUZZLE.guessRole(starterIndex, character.criminal)` — i.e. always with the CORRECT answer. It therefore always takes the `isCorrect` branch and can never increment a mistake counter. No special-casing is needed for it (see task 4C).
- puzzle-ui.js:695 / 733 — the stats bar reads `NARSH_PUZZLE.getMistakes()` (global count). Unchanged by this plan.
- Module-level mutable state in puzzle.js uses `let` (`let colorTags = {}`), while CLAUDE.md's "const throughout" rule governs new local/element bindings. New module state follows the local `let` pattern; do not "fix" it to `const`.
</scouted_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .color-tag-hit corner target styles</name>
  <files>narsh2026/puzzles/puzzle.css</files>
  <action>
Add the corner hit-target styling. Repo conventions: 2-space indent, existing custom properties (`--color-terracotta`, `--radius-md`, `--transition-fast`), no new files.

1. Immediately BEFORE the existing `/* Color tag dot */` block at puzzle.css:244, add a `/* Color tag hit target (top-right corner) */` block for `.color-tag-hit`:
   - `position: absolute; top: 0; right: 0;`
   - `width: 30px; height: 30px;` (per design decision 1: comfortably larger than the 14px dot, ~25% of a desktop card's width, still leaving the bulk of the card for the guess-dialog click)
   - `box-sizing: border-box; padding: 0; border: none; background: transparent;`
   - `border-radius: 0 var(--radius-md) 0 var(--radius-md);` so the target follows the card's rounded corner
   - `cursor: pointer;`
   - `touch-action: manipulation;` and `-webkit-tap-highlight-color: transparent;` to avoid the mobile grey flash and tap delay
   - `z-index: 1;` so it sits above the front face content
   - Do NOT set `pointer-events: none` here — this element is the one thing on the front face that must receive clicks.

2. Add `transform var(--transition-fast)` to the existing `.color-tag` `transition` list (puzzle.css:256) so the hover affordance animates.

3. Add a hover affordance rule INSIDE the existing `@media (hover: hover)` block (puzzle.css:142-152):
   `.color-tag-hit:hover + .color-tag { transform: scale(1.25); border-color: var(--color-terracotta); }`
   This depends on DOM order — Task 2 must insert `.color-tag-hit` as the immediately-preceding sibling of `.color-tag`.

4. Add the flipped guard (design decision 5) right after the existing `/* Card flipped: no pointer interaction */` rule at puzzle.css:154-157:
   `.puzzle-card.flipped .color-tag-hit { pointer-events: none; }`
   Rationale comment to include in the CSS: the flipped front face is back-facing under `backface-visibility: hidden` so browsers already skip it, but this makes the intent explicit and independent of hit-testing behavior.

5. Inside the existing `@media (prefers-reduced-motion: reduce)` block (puzzle.css:631-660), add:
   `.color-tag-hit:hover + .color-tag { transform: none; }`

6. Inside the existing `@media (max-width: 480px)` block (puzzle.css:673-677), add `.color-tag-hit { width: 28px; height: 28px; }` — on a ~71px-wide card at a 320px viewport, 30px would claim too much of the card face.
  </action>
  <verify>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*/\*' narsh2026/puzzles/puzzle.css | grep -c 'color-tag-hit' | awk '$1>=5{print "PASS "$1} $1<5{print "FAIL "$1; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -q 'flipped .color-tag-hit' narsh2026/puzzles/puzzle.css && grep -q 'width: 28px' narsh2026/puzzles/puzzle.css && echo "PASS guards"</automated>
  </verify>
  <done>puzzle.css defines `.color-tag-hit` at 30px (28px under 480px), a `:hover + .color-tag` affordance inside the hover-capable media query, a `pointer-events: none` guard for flipped cards, and a reduced-motion override. `.color-tag` transitions `transform`.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the corner target in renderGrid and refresh stale comments</name>
  <files>narsh2026/puzzles/puzzle-ui.js, narsh2026/puzzles/puzzle.js</files>
  <action>
Repo conventions: 2-space indent, double quotes, semicolons, `const` for new local bindings, `El` suffix on DOM element variables, plain DOM APIs, no build step.

**A. Create the hit element (puzzle-ui.js, inside `renderGrid`, at the color-tag block around line 467-470).**

Insert the hit target BEFORE the existing `tagDotEl` so it is the immediately-preceding sibling of `.color-tag` (required by the CSS `+` selector from Task 1). Name it `tagHitEl` per the `El` convention:
   - `const tagHitEl = document.createElement("span");`
   - `tagHitEl.className = "color-tag-hit";`
   - `tagHitEl.setAttribute("aria-hidden", "true");` — see the a11y note below
   - `tagHitEl.title = "Click to cycle color marker";` — the only discoverability affordance, since the page has no instructions copy
   - `frontEl.appendChild(tagHitEl);` then the existing `tagDotEl` creation/append follows unchanged.

Leave `tagDotEl.className = "color-tag"` exactly as-is: the restore loop at puzzle-ui.js:421-428 does `querySelector(".color-tag")` and must keep matching exactly one element per card.

**B. Add handlers on `tagHitEl`, placed immediately after the existing cardEl `keydown` handler (i.e. before the `contextmenu` handler at line 509) so the color-marker handlers read as one group.**

1. Click handler:
   - `event.stopPropagation();` FIRST (design decision 2) so `handleCardClick` never runs and the guess dialog never opens.
   - Guard: `if (cardEl.classList.contains("flipped")) return;` (design decision 5) — a flipped card's front face and its dot are not visible, so cycling there would be an invisible state change.
   - `const tagColor = NARSH_PUZZLE.cycleColorTag(i);` then `tagDotEl.style.backgroundColor = tagColor;` — identical to the existing contextmenu handler, including the `"transparent"` (index 0) clear step.

2. Touchstart handler (`{ passive: true }`), doing ONLY `event.stopPropagation();`:
   - The card's own `touchstart` listener (line 518) starts a 300ms long-press timer that also cycles. Without this, a corner tap held >300ms would cycle once from the long-press AND once from the synthesized click = double cycle. Stopping propagation at the hit target prevents the card timer from ever starting, so a corner touch always cycles exactly once via the click handler. `stopPropagation` is legal inside a passive listener (only `preventDefault` is not).

Do NOT add a `contextmenu` handler to `tagHitEl`: right-clicking the corner bubbles to the card's existing handler and cycles once, which is the desired behavior.

**C. Accessibility note (design decision 4) — record this as a code comment above the `tagHitEl` creation, and repeat it in the SUMMARY.**

The hit target is deliberately NOT focusable (no `tabindex`, `aria-hidden="true"`, a `<span>` not a `<button>`). Reasons: the grid renders one target per character card, so making them focusable would roughly double the tab stops on the page for a secondary note-taking affordance; and the card's own `role="button"` / `tabindex="0"` / Enter-Space path to the guess dialog must stay unchanged. Keyboard-only users retain the pre-existing path to color cycling: focus the card and press the context-menu key (or Shift+F10), which dispatches `contextmenu` on the focused card and hits the untouched handler at line 510. Net a11y change: zero regressions, no new tab stops, no new labels needed.

Do NOT modify the cardEl `click`, `keydown`, `contextmenu`, `touchstart`, `touchmove`, or `touchend` handlers (design decision 3 — this adds a way in, it removes nothing).

**D. Refresh the three stale comments (design decision 6).** Repo-wide grep confirms these are the ONLY places mentioning right-click/long-press; there is no user-facing copy to change, so do not add on-page instructions for the color marker.
   - `puzzle.js:9` -> note the 7 colors cycle via a corner click/tap, with right-click (desktop) and long-press (mobile) as alternates.
   - `puzzle-ui.js:509` -> "Desktop: right-click also cycles color tag (alternate to corner click)".
   - `puzzle-ui.js:516` -> "Mobile: long-press (300ms) also cycles color tag (alternate to corner tap)".
  </action>
  <verify>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && node --check narsh2026/puzzles/puzzle-ui.js && node --check narsh2026/puzzles/puzzle.js && echo "PASS syntax"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle-ui.js | grep -c 'color-tag-hit' | awk '$1>=1{print "PASS hit "$1} $1<1{print "FAIL"; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle-ui.js | grep -c '"contextmenu"' | awk '$1==1{print "PASS contextmenu preserved"} $1!=1{print "FAIL "$1; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle-ui.js | grep -c '"touchstart"' | awk '$1==2{print "PASS two touchstart listeners"} $1!=2{print "FAIL "$1; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle-ui.js | grep -c 'cycleColorTag' | awk '$1>=3{print "PASS "$1} $1<3{print "FAIL "$1; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle-ui.js | grep -c 'tabindex' | awk '$1>=1{print "PASS card tabindex intact"} $1<1{print "FAIL"; exit 1}'</automated>
  </verify>
  <done>`renderGrid` appends a non-focusable `.color-tag-hit` span before the `.color-tag` dot on every card, with a click handler that stops propagation, skips flipped cards, and cycles the tag, plus a passive touchstart handler that stops propagation to suppress the card long-press. All pre-existing card handlers are byte-identical apart from comment text. The three right-click/long-press comments now describe the corner interaction as primary.</done>
</task>

<task type="auto">
  <name>Task 3: Credit Clues by Sam above the grid and point at its tutorial</name>
  <files>narsh2026/puzzles/index.html, narsh2026/puzzles/puzzle.css</files>
  <action>
Add a credit/attribution line above the puzzle game that links to Clues by Sam and tells a newcomer a tutorial lives there.

**A. Placement decision: static markup in `index.html`, inserted BETWEEN `<h1>Clues by Narsh</h1>` (line 31) and `<p class="puzzle-identity-clue">` (line 32).**

Rationale to preserve in the SUMMARY:
- Above the identity clue, not below it, because the identity clue is *diegetic* — it is the in-fiction opening clue for the case, populated from `NARSH_PUZZLE_DATA.IDENTITY_CLUE`. Meta/attribution text belongs grouped with the title; dropping it between the story clue and the grid would interrupt the narrative beat exactly where the player starts playing.
- Second reason: `.puzzle-identity-clue:empty { display: none; }` means the clue paragraph disappears when no identity clue is set. A credit line placed after it would inherit awkward stacked spacing in that case; placed before it, the credit always sits at a fixed distance from the `<h1>`.

**B. Static HTML, not JS injection.** Choose static markup because: the content is fixed copy with no data dependency (unlike `DAILY_PUZZLES`, which is data-driven and therefore rendered by `renderDailyPuzzles`); the sibling `<h1>` is already static, so the credit inherits exactly the same auth-gating behavior as the rest of `<main>`; and JS injection would tie a piece of page chrome to `NARSH_PUZZLE_UI.init()`, which only runs after `requireAuth()` succeeds. Do NOT add an entry to `DAILY_PUZZLES` or touch `renderDailyPuzzles` — the existing bottom-of-page "Clues by Sam" card at `puzzle-data.js:224` stays exactly as it is; this is an additional, separate credit line.

**C. Exact markup to insert** (2-space indent matching its siblings, double-quoted attributes):

A `<p class="puzzle-credit">` containing: the text `Inspired by `, then an `<a>` reading `Clues by Sam`, then ` -- new to this kind of puzzle? There's a tutorial over there.`

Anchor attributes:
   - `href="https://cluesbysam.com/"` — the ROOT url only. Do not guess a `/tutorial` path; an unverifiable deep link that 404s is worse than one extra click, and the copy already tells the reader what to look for.
   - `target="_blank"`
   - `rel="noopener noreferrer"`
   - `aria-label="Clues by Sam -- opens in new tab"` — this exact phrasing matches the house pattern used for every external link in `renderDailyPuzzles` (puzzle-ui.js:930). Use `aria-label` rather than a visually-hidden span: grep confirms this repo has NO `.sr-only` / `.visually-hidden` utility, so a hidden-span approach would mean inventing a new pattern for one link.

Copy notes: use ASCII `--` for the em dash, matching `<title>Clues by Narsh -- Narsh 2026</title>` and the existing aria-labels. Keep the contraction ("There's") — the site's voice is warm and conversational.

**D. Styling in `narsh2026/puzzles/puzzle.css`**, added directly after the `.puzzle-identity-clue:empty` rule (lines 36-38) so the page-header rules stay grouped:

   - `.puzzle-credit`: `text-align: center;`, `font-size: var(--font-size-sm);`, `color: var(--color-text-secondary);`, `max-width: 32rem;` (matches `.puzzle-identity-clue`), `margin: 0 auto var(--space-sm) auto;` — the `<h1>` already contributes `--space-sm` below itself and the identity clue keeps its own `--space-md` below, so the rhythm stays tight and the credit never competes with the 28px heading.
   - `.puzzle-credit a`: `color: var(--color-terracotta); text-decoration: underline; text-underline-offset: 2px; transition: color var(--transition-fast);`
   - `.puzzle-credit a:hover, .puzzle-credit a:focus`: `color: var(--color-text-primary);`
   - `.puzzle-credit a:focus-visible`: `outline: 2px solid var(--color-terracotta); outline-offset: 2px;` — matches the existing focus treatment at puzzle.css:547-550 / 625-628.
   - Use only existing tokens; introduce no new custom properties and no hard-coded hex values.
  </action>
  <verify>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -q 'class="puzzle-credit"' narsh2026/puzzles/index.html && grep -q 'https://cluesbysam.com/' narsh2026/puzzles/index.html && grep -q 'tutorial' narsh2026/puzzles/index.html && echo "PASS credit copy"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -q 'rel="noopener noreferrer"' narsh2026/puzzles/index.html && grep -q 'target="_blank"' narsh2026/puzzles/index.html && grep -q 'aria-label="Clues by Sam -- opens in new tab"' narsh2026/puzzles/index.html && echo "PASS link hygiene"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && python3 -c "
body = open('narsh2026/puzzles/index.html').read()
body = body[body.index('<main'):]
i_h1 = body.index('<h1>')
i_credit = body.index('puzzle-credit')
i_clue = body.index('puzzle-identity-clue')
i_game = body.index('puzzle-game')
assert i_h1 < i_credit < i_clue < i_game, 'credit line is misplaced'
assert body.count('cluesbysam.com') == 1, 'unexpected extra cluesbysam link in index.html'
print('PASS placement: h1 -> credit -> identity clue -> game')
"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*/\*' narsh2026/puzzles/puzzle.css | grep -c 'puzzle-credit' | awk '$1>=4{print "PASS css "$1} $1<4{print "FAIL "$1; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -c 'Clues by Sam' narsh2026/puzzles/puzzle-data.js | awk '$1==1{print "PASS DAILY_PUZZLES entry untouched"} $1!=1{print "FAIL "$1; exit 1}'</automated>
  </verify>
  <done>`index.html` has a static `<p class="puzzle-credit">` between the `<h1>` and the identity clue, reading "Inspired by [Clues by Sam] -- new to this kind of puzzle? There's a tutorial over there.", where the link points at `https://cluesbysam.com/` with `target="_blank"`, `rel="noopener noreferrer"`, and the house-standard `aria-label` new-tab announcement. `puzzle.css` styles it as a small secondary centered line using existing tokens, with hover and focus-visible states. The bottom "Daily Puzzles We Love" Clues by Sam card is unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Track per-card mistakes and make the share grid reflect player accuracy</name>
  <files>narsh2026/puzzles/puzzle.js</files>
  <action>
The completion grid currently encodes each character's ROLE (`characters[i].criminal ? red : green`, puzzle.js:203). Change it to encode the PLAYER's accuracy. This requires new state — there is no per-card mistake record today.

Grid semantics are LOCKED and binary — do not invent a graded scale:
- 🟩 green = the player never guessed wrong on that card (clean first-try solve)
- 🟥 red = the player guessed wrong on that card at least once
Keep the existing 4-tiles-per-row layout and the existing header/footer lines of the share text byte-identical. Do NOT add a legend line.

**A. Add per-card mistake state (mirrors the existing `colorTags` shape exactly, so it serializes with no Set/Array conversion).**

1. puzzle.js:29 area — beside `let colorTags = {};` add:
   `// Per-card mistakes: cardIndex -> count of wrong guesses on that card`
   `let mistakesByCard = {};`
   Use `let`, matching the surrounding module state. (CLAUDE.md's "const throughout" governs new local/element bindings; module-level mutable state here is already `let`.)
2. `guessRole` incorrect branch (puzzle.js:361-368) — directly beside the existing `mistakes++;`, add:
   `mistakesByCard[cardIndex] = (mistakesByCard[cardIndex] || 0) + 1;`
   Keep the existing `onMistakeCallback(mistakes)` call and its global argument unchanged — the stats bar (puzzle-ui.js:695/733) depends on the global count.
3. `saveState()` (puzzle.js:218-234) — add `mistakesByCard: mistakesByCard,` immediately after the `colorTags` entry.
4. `init()` (puzzle.js:260-301) — in the restored branch add `mistakesByCard = saved.mistakesByCard || {};` after the `colorTags` line; in the fresh branch add `mistakesByCard = {};` after `colorTags = {};`.
5. `reset()` (puzzle.js:381-395) — add `mistakesByCard = {};` after `colorTags = {};`.
6. Module return object (puzzle.js:397-424) — export an accessor beside `getMistakes`:
   `const getCardMistakes = (cardIndex) => mistakesByCard[cardIndex] || 0;`
   and add `getCardMistakes,` to the returned object. Nothing consumes it yet (the share grid reads the local directly), but it makes the per-card data available to the UI without a second refactor later.

**B. Rewrite the grid loop in `generateShareText()` (puzzle.js:197-205).**

Keep `const characters = puzzleData.CHARACTERS;` and the `characters.length` loop bounds and the `if (i > 0 && i % 4 === 0) text += "\n";` row break exactly as they are. Replace ONLY the tile expression:
   from `characters[i].criminal ? "\u{1F7E5}" : "\u{1F7E9}"`
   to a check on `mistakesByCard[i]` being greater than 0 -> red `"\u{1F7E5}"`, else green `"\u{1F7E9}"`.
Keep the `"\u{...}"` escape style (do not paste literal emoji). Add a short comment above the loop stating that tiles encode player accuracy, not character roles, so nobody "restores" the old behavior.

Lines 194-195 (title + time/mistakes/hints) and line 207 (footer) must not change.

**C. The auto-revealed starter card renders GREEN, with no special-casing.**

Verified: `autoRevealStarterCard()` (puzzle-ui.js:785-818) calls `guessRole(starterIndex, character.criminal)` — always the correct answer — so it always takes the `isCorrect` branch and can never increment `mistakesByCard`. It therefore falls through to green naturally. This is the right reading: the player made no mistake there. Do not add an exception, a fourth emoji, or a skip; just note the behavior in the SUMMARY so a future reader does not think it was overlooked.

**D. Legacy saves: DO NOT bump `DATA_VERSION`. Handle the missing key by defaulting to `{}`.**

`loadState()` (puzzle.js:236-248) validates only `state.version` against `puzzle-data.js:10`'s `DATA_VERSION = "4"`. A save written before this change has no `mistakesByCard` key.
   - Chosen fallback: `saved.mistakesByCard || {}` (step A4). Reading it cannot throw, and every tile renders green.
   - Known imperfection, accepted: a pre-existing save with `mistakes > 0` will show an all-green grid. It is NOT a silent lie — line 2 of the share text still prints the true global count ("❌ 3 mistakes"), so the grid and the header visibly disagree and the player can see which is authoritative. The state self-heals on the next "Play Again" / reset.
   - **Rejected alternative: bumping `DATA_VERSION` to "5".** That makes `loadState()` return null for every existing save, so every guest with a game in progress loses all of it — flipped cards, timer, color markers. Destroying a guest's progress to fix a cosmetic grid on an already-finished game is the worse trade. Leave `DATA_VERSION` at "4" and do not touch `puzzle-data.js`.

**E. Update the stale module docstring (puzzle.js:1-4).** It currently lists "mistake tracking" among the module's responsibilities. Make it say the module tracks mistakes both globally and per card, and that the share grid encodes per-card accuracy.
  </action>
  <verify>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && node --check narsh2026/puzzles/puzzle.js && echo "PASS syntax"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -v '^\s*//' narsh2026/puzzles/puzzle.js | grep -c 'mistakesByCard' | awk '$1>=7{print "PASS wiring "$1} $1<7{print "FAIL "$1" (need decl, increment, save, restore, fresh-init, reset, accessor)"; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -F -c 'criminal ? "\u{1F7E5}"' narsh2026/puzzles/puzzle.js | awk '$1==0{print "PASS role-based tiles removed"} $1!=0{print "FAIL old expression still present"; exit 1}'</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && grep -q 'const DATA_VERSION = "4";' narsh2026/puzzles/puzzle-data.js && echo "PASS DATA_VERSION not bumped (in-progress saves survive)"</automated>
    <automated>cd /Users/nataliefleury/programming/arashrai.github.io && node -e '
const fs = require("fs"), vm = require("vm");
// Stubs verified by the planner: puzzle.js registers a visibilitychange listener at
// module scope and calls performance.now() in startTimer. localStorage is left undefined
// on purpose -- saveState/loadState already swallow the ReferenceError, so the run
// exercises the fresh-game path.
const ctx = vm.createContext({ console, performance, document: { addEventListener() {}, hidden: false }, window: {} });
for (const f of ["puzzle-data.js", "puzzle.js"]) vm.runInContext(fs.readFileSync("narsh2026/puzzles/" + f, "utf8"), ctx);
const P = vm.runInContext("NARSH_PUZZLE", ctx), D = vm.runInContext("NARSH_PUZZLE_DATA", ctx);
P.init(D);
const total = D.getTotalCharacters();
let starter = -1;
for (let i = 0; i < total; i++) { const c = D.getCharacter(i); if (c && c.paths.some(p => p.length === 0)) { starter = i; break; } }
if (starter < 0) throw new Error("no starter card found");
P.guessRole(starter, D.getCharacter(starter).criminal);
let victim = -1;
for (let i = 0; i < total; i++) { if (i === starter) continue; const c = D.getCharacter(i); if (c && c.paths.some(p => p.every(x => P.getFlippedCards().has(x)))) { victim = i; break; } }
if (victim < 0) throw new Error("no deducible second card found");
const wrong = P.guessRole(victim, !D.getCharacter(victim).criminal);
if (wrong.result !== "incorrect") throw new Error("setup failed: " + wrong.result);
const cells = Array.from(P.getShareText().split("\n\n")[1].replace(/\n/g, ""));
const reds = cells.filter(ch => ch === "\u{1F7E5}").length;
if (cells.length !== total) throw new Error("grid has " + cells.length + " tiles, expected " + total);
if (reds !== 1) throw new Error("expected exactly 1 red tile, got " + reds);
if (cells[victim] !== "\u{1F7E5}") throw new Error("red tile is not at the mis-guessed index " + victim);
if (cells[starter] !== "\u{1F7E9}") throw new Error("auto-revealed starter should be green");
if (P.getCardMistakes(victim) !== 1 || P.getCardMistakes(starter) !== 0) throw new Error("getCardMistakes accessor wrong");
console.log("PASS grid reflects player accuracy (1 red at index " + victim + ", starter green)");
'</automated>
  </verify>
  <done>`puzzle.js` tracks `mistakesByCard` (declared, incremented in the incorrect branch, saved, restored with a `|| {}` fallback, cleared on reset, exposed via `getCardMistakes`). `generateShareText()` emits red only for cards with at least one wrong guess and green otherwise, with header/footer/row-break logic untouched. `DATA_VERSION` is still "4" so no in-progress save is invalidated. The headless simulation proves the red tile lands at the mis-guessed index and the auto-revealed starter stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human verification of all three changes</name>
  <action>Pause and hand the verification steps below to the developer. Do not self-approve; wait for the resume signal.</action>
  <what-built>(1) Corner color-marker target on puzzle cards: a ~30px transparent hit area in the top-right of each card front, with a hover affordance, a flipped-card guard, and preserved right-click / long-press / keyboard behavior. (2) An "Inspired by Clues by Sam" credit line above the grid pointing newcomers at the tutorial. (3) The completion/share emoji grid now encodes player accuracy (green = first-try, red = mis-guessed) instead of character roles, backed by new per-card mistake tracking.</what-built>
  <how-to-verify>
Serve the site locally and open the puzzle page (auth-gated, so log in with a valid tier):

1. `cd /Users/nataliefleury/programming/arashrai.github.io && python3 -m http.server 8000`
2. Visit `http://localhost:8000/narsh2026/puzzles/` and authenticate.

Desktop checks — corner marker:
3. Hover the top-right corner of an unflipped card — the dot should grow slightly and the border turn terracotta; the cursor stays a pointer; a "Click to cycle color marker" tooltip appears after a beat.
4. Left-click the corner repeatedly — the dot cycles red -> orange -> yellow -> green -> blue -> purple -> cleared. The guess dialog must NEVER open from these clicks.
5. Click the CENTER of the same card — the Criminal/Innocent guess dialog opens as before. Dismiss it.
6. Right-click the card center — the dot still cycles (alternate path intact).
7. Tab to a card and press Enter — the guess dialog opens (keyboard path unchanged). Tab once more: focus should move to the NEXT CARD, not to a corner target.
8. Reload the page — the colors you set are still there.
9. Flip a card by answering its dialog, then click its top-right corner — nothing should happen (no dialog, no color change).

Desktop checks — Clues by Sam credit:
10. Confirm the credit line sits directly under the "Clues by Narsh" heading, above the italic identity clue, and reads: "Inspired by Clues by Sam -- new to this kind of puzzle? There's a tutorial over there."
11. It should read as a quiet secondary line — smaller than the heading, not competing with it, comfortably centered on both a wide window and a narrow one.
12. Hover the "Clues by Sam" link — the colour shifts and the underline reads clearly. Click it: `https://cluesbysam.com/` opens in a NEW tab with the puzzle page intact behind it (progress not lost).
13. Tab to the link — a terracotta focus ring appears. With VoiceOver (Cmd+F5) on the link, it should announce "Clues by Sam -- opens in new tab".
14. Scroll to the bottom "Daily Puzzles We Love" section — the original Clues by Sam card is still there and still works. (Duplication is intentional: credit at the top, directory at the bottom. Say so if it feels redundant to you.)

Desktop checks — completion grid (the slowest check; budget a full playthrough):
15. Start a fresh game (Play Again -> Start Over if needed). Play to completion, deliberately guessing WRONG on exactly two cards you can identify by position — e.g. the 2nd card of row 1 and the 3rd card of row 3 — and getting every other card right first try.
16. On the "Case Closed!" modal, the emoji grid should show red at exactly those two positions (row-major, 4 per row) and green everywhere else.
17. Sanity-check the direction of the change: pick a card you know was a CRIMINAL that you got right first try — it must be GREEN. Under the old behavior it would have been red. If criminals are still red, the change did not take.
18. The stats line above the grid should still read the true totals ("... with 2 mistakes and N hints"), and the grid's red count should match that only when your mistakes were on distinct cards (two wrong guesses on the SAME card = 2 mistakes but 1 red tile — expected).
19. Click "Share Results" — the button flips to "Copied!". Paste into a text field: the pasted grid must be identical to the modal preview.
20. Reload the page with the game complete — the modal reappears and the grid is unchanged (per-card data survived the round-trip through localStorage).

Legacy-save check (proves old saves do not break):
21. With a completed game in place, open DevTools -> Application -> Local Storage -> `narsh-puzzle-state`. Delete just the `"mistakesByCard"` key from the JSON, save, and reload.
22. The page must load normally and the completion modal must render an ALL-GREEN grid with no console errors — even though the stats line still reports your real mistake count. That mismatch is the accepted, documented tradeoff for not wiping in-progress saves; confirm it looks tolerable to you rather than alarming.

Mobile checks (DevTools device toolbar, iPhone-size, touch emulation on, or a real phone):
23. Tap the top-right corner — the color advances by exactly ONE step per tap (watch for a double-jump, which would mean the long-press suppression failed).
24. Press and HOLD the corner for ~1s, release — still exactly one step.
25. Press and hold the card CENTER for ~1s — the color cycles via long-press (alternate path intact).
26. Tap the card center — the guess dialog opens.
27. Confirm the corner target is comfortable to hit at phone width without feeling like it swallows the card.
28. Confirm the credit line wraps to at most two lines at phone width and does not push the grid uncomfortably far down the page.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what misbehaved (include which numbered step)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> DOM event handlers | Clicks/taps on the puzzle grid; no network or server involvement |
| localStorage <-> puzzle state | Persisted `colorTags`, `mistakes`, and now `mistakesByCard` read back on load |
| page -> third-party origin | New outbound link to `cluesbysam.com` opened in a new tab |
| game state -> clipboard | Share text copied to the system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-pa8-01 | Tampering | `colorTags` in localStorage | accept | Pre-existing boundary; color tags are client-side player notes with no authority over game truth or auth. The corner handler calls the same `cycleColorTag(i)` bounded to indices 0-6. |
| T-pa8-02 | Information disclosure | `tagHitEl.title` tooltip | accept | Static literal string containing no puzzle answers, no character data, no user data. |
| T-pa8-03 | Tampering | DOM injection via new element | mitigate | The hit element is created with `document.createElement` and only static literal attributes; no `innerHTML`, no interpolation of character or user data. |
| T-pa8-04 | Elevation of privilege | `target="_blank"` link to cluesbysam.com | mitigate | `rel="noopener noreferrer"` blocks reverse-tabnabbing (`window.opener` access from the destination) and suppresses the referrer, so the third-party origin cannot script the auth-gated puzzle tab or learn the gated URL. |
| T-pa8-05 | Information disclosure | Referrer leakage of the gated puzzle URL | mitigate | Covered by `noreferrer` in the same `rel` attribute; the destination sees no `Referer` header. |
| T-pa8-06 | Tampering | `mistakesByCard` in localStorage | accept | A player can hand-edit the key to fake a perfect share grid. Same class and same blast radius as the pre-existing `mistakes` counter: client-side vanity on a wedding-guest puzzle, no server-side scoring, no auth impact. |
| T-pa8-07 | Information disclosure | Share text on the clipboard | accept | The grid now encodes the player's own accuracy rather than which characters are criminals — strictly LESS spoiler content than the current role-based grid. Net reduction in disclosure. |
</threat_model>

<verification>
- `node --check` passes on both modified JS files.
- Corner click cycles the marker; card-body click still opens the guess dialog.
- Corner tap cycles exactly once (no long-press double-cycle).
- `contextmenu` and long-press handlers on the card are unchanged and still work.
- Card `role="button"` / `tabindex="0"` / Enter-Space behavior unchanged; no new tab stops introduced by the corner target.
- Flipped cards ignore the corner target.
- Color tags still persist and restore across reload (`querySelector(".color-tag")` still resolves to exactly one dot per card).
- Credit line renders between the `<h1>` and the identity clue, links to `https://cluesbysam.com/` in a new tab with `rel="noopener noreferrer"`, and announces "opens in new tab" to screen readers.
- The bottom "Daily Puzzles We Love" Clues by Sam entry is unmodified.
- Headless simulation: the share grid has exactly one red tile at the mis-guessed index, the auto-revealed starter is green, and tile count equals character count.
- `DATA_VERSION` is unchanged at "4"; a save lacking `mistakesByCard` loads without throwing and renders an all-green grid.
- Share modal preview and clipboard payload are the same string (both call `getShareText()`).
</verification>

<success_criteria>
- A single left-click or tap on a card's top-right corner cycles that card's color marker, on desktop and mobile.
- A guest who has never seen this puzzle type can find the Clues by Sam tutorial from above the grid, without scrolling past the game.
- The completion grid tells the player how THEY did — green for first-try solves, red for cards they got wrong — and no longer reveals which characters were criminals.
- No regression to guess dialog, inspect-mode dim toggle, keyboard access, right-click, long-press, stats bar, timer, or persistence; no existing save is invalidated.
- Changes confined to `puzzle.css`, `puzzle-ui.js`, `puzzle.js`, and `index.html`; no new files, no dependencies, no build step.
</success_criteria>

<output>
After completion, create `.planning/quick/260731-pa8-puzzle-page-click-top-right-corner-of-a-/260731-pa8-SUMMARY.md`.

Record explicitly in the summary:
- The a11y decision: hit target is non-focusable and `aria-hidden`, no new tab stops, keyboard color cycling remains via the context-menu key / Shift+F10 on the focused card.
- The flipped-card finding: `backface-visibility: hidden` already excludes the flipped front face from hit testing; the plan adds a CSS `pointer-events: none` guard plus a JS `classList.contains("flipped")` guard so the behavior is explicit rather than browser-dependent.
- The instructions-copy finding: no user-facing text anywhere in the repo mentions right-click, so that requirement was satisfied by updating three code comments plus adding the `title` tooltip; no on-page copy was invented for the color marker.
- The credit-line placement rationale (above the diegetic identity clue, static markup rather than JS injection) and the deliberate choice of the root `cluesbysam.com` URL over an unverified `/tutorial` deep link.
- That Clues by Sam now appears twice on the page by design (top credit + bottom directory card), so a future reader does not "clean up" the duplication by accident.
- The new `mistakesByCard` state: where it is incremented, saved, restored, and cleared, and that `getCardMistakes` is exported but not yet consumed by the UI.
- That the auto-revealed starter card renders green by construction (`autoRevealStarterCard` always guesses correctly), with no special-casing.
- The legacy-save tradeoff: `DATA_VERSION` deliberately left at "4" so in-progress games survive; pre-existing saves render an all-green grid while the header line still reports the true mistake count, and this self-heals on the next reset. Bumping the version was considered and rejected because it would wipe every guest's in-progress game.
</output>
</content>
